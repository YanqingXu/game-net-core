# Commit-Bound Evidence Ledger Template

Use one copied section per integrated architecture slice. Evidence is attached
to an immutable commit; a dirty-worktree result may be recorded as local
preflight but cannot be promoted to commit or remote evidence.

## Slice Record Template

| Field | Required value |
| --- | --- |
| Slice | Plan identifier and one-sentence scope |
| Commit | Full 40-character commit SHA; never `HEAD` |
| Parent/baseline | Exact comparison SHA and baseline document |
| Intent/ADR | Active intent paths and accepted architecture decision |
| Owner/lifetime | Owner thread, owner/releaser, re-entry, cross-thread path |
| Public surface | `none`, or exact reviewed API diff artifact |
| Changed production paths | Explicit path list |
| Focused contracts | Exact command, test names, result, duration |
| Windows full gate | Exact command/result and artifact or local log identity |
| Linux full gate | Exact command/result and artifact or local log identity |
| Benchmark/capacity | Scenario, paired sample count, result, decision |
| Sanitizer/race evidence | Required lane and result, or reason not applicable |
| Review | Reviewer identity, scope, findings, disposition |
| Remote evidence | Run/attempt/artifact/hash, or explicitly `not collected` |
| Decision | `integrate`, `fix`, `revert`, or `promotion-only follow-up` |

## Integrity Rules

1. Record the commit only after the tree is committed and clean.
2. Do not copy evidence from an ancestor unless the changed path audit proves it
   remains applicable and the record labels that proof.
3. Local and remote results remain separate. Workflow presence is not evidence.
4. A benchmark beyond its investigation threshold requires paired reruns and a
   written accept/fix decision.
5. Failed or superseded evidence remains visible with its disposition.
6. Release/endurance evidence is promotion-only unless a slice changes the
   corresponding validator or lifecycle contract.

ARCH-G1 establishes this template. IOE-R1 is the first slice required to append
a completed, exact-commit record.

## IOE-R1 Source-Private Adapter — 2026-08-19

| Field | Evidence |
| --- | --- |
| Slice | IOE-R1 first vertical slice: source-private `IoEngine`, native-Poller adapter, EventLoop routing, lifecycle/capability contract |
| Commit | `fc03d4f492ed5481fd8bfd082f90429c4f0ff6a4` |
| Parent/baseline | Parent `65b3051ce97bcf44ee75eb62ec270c2ab38b5abb`; runtime comparison `669ebb0a7c5c475dea74b12275c66a2ce1876804`; `docs/development/io_engine_baseline.md` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`, `intents/architecture/runtime_models.intent.md`, `docs/architecture/io_engine_runtime_model_adr.md` |
| Owner/lifetime | One EventLoop owner thread owns the dual-interface Engine object through its byte-stable `unique_ptr<Poller>` storage. Wait, registration, completion tracking, dispatch state, quiesce, and release are owner-only. `wakeup()` is the only cross-thread Engine operation. EventLoop invokes re-entrant user callbacks and destroys the backend after attached owner-thread teardown. Existing completion leases remain retained by the native backend until terminal dequeue. |
| Public surface | `none`; no installed header changed. `tests/api/test_public_api_manifest.py` and the same-line `compare_public_api_manifest.py` blocking gate passed with the reviewed v0.3 snapshot. |
| Changed production paths | `src/core/net/detail/IoEngine.h`; `src/core/net/detail/PollerIoEngineAdapter.cc`; `src/core/net/EventLoop.cc`; `src/core/net/detail/EventLoopControlRegistry.h`; `src/core/net/detail/EventLoopIocpAssociationHarness.h`; `src/core/CMakeLists.txt` |
| Focused contracts | Windows and Linux: `ctest --test-dir <release-build> -R '^(contract.io_engine.test_io_engine_poller_adapter\|contract.poller.test_poller_contract\|contract.event_loop.test_event_loop\|contract.channel.test_channel_active_batch_lifetime\|contract.tcp_connection.test_tcp_connection_completion_drain\|integration.tcp.test_iocp_quit_completion_drain\|integration.tcp.test_iocp_accept_connect_quit_completion_drain)$' --output-on-failure`; 7/7 passed in 0.32 s Windows and 0.29 s Linux. |
| Windows full gate | Exact clean commit; `ctest --test-dir build-perf-r1-fix -C Release --output-on-failure`; 122/122 passed in 51.73 s. |
| Linux full gate | Exact clean commit; `ctest --test-dir build-wsl-perf-r1-fix --output-on-failure`; 122/122 passed in 49.42 s under WSL/GCC 13.3. |
| Benchmark/capacity | Exact clean commit, one warmup plus three alternating echo pairs against `669ebb0`: Windows throughput 60.797 -> 63.402 MiB/s (+4.29%), P99 74.8 -> 66.7 us (-10.83%); Linux throughput 48.590 -> 48.542 MiB/s (-0.10%), P99 125.101 -> 96.946 us (-22.51%). No regression crossed the 5% investigation threshold; integrate. |
| Sanitizer/race evidence | Not applicable to this adapter-only local integration decision: it adds no shared queue, callback owner, operation storage, or lifetime release rule. No sanitizer result is claimed; sanitizer/race evidence remains required when IOE-C1 changes completion storage and retirement. |
| Review | Codex `/root` self-review covered intent, public contract, owner/thread affinity, lifetime, re-entry, cross-thread wakeup, tests, and performance. The stable-header constraint found during review caused the adapter to preserve the byte-identical EventLoop/Poller headers. Independent ARCH-G1 review remains pending and is not claimed here. |
| Governance | Exact clean commit; all 26 repository/API/CI guard scripts passed; 32 active intents and 156 explicit verification paths; same-line public API blocking gate passed. |
| Remote evidence | `not collected`; local evidence is not promoted to CI or release evidence. |
| Decision | `integrate` this first IOE-R1 slice; IOE-R1 remains open for explicit register/submit/cancel admission results, stale-notice and budget-exhaustion contracts, and option layering. |

## IOE-R1 Contract Closure — 2026-08-19

| Field | Evidence |
| --- | --- |
| Slice | IOE-R1 closure: typed admission/operation outcomes, owner-only mutation, completion obligation drain, callback containment, stale-notice/budget continuation, and backend-capacity option layering |
| Commit | `8bb14e72d8935879396d12a7a51c891311aa2a78` |
| Parent/baseline | Parent `c9c9c47b73370b6d92541b179a0023048a5ff3f6`; runtime comparison `669ebb0a7c5c475dea74b12275c66a2ce1876804`; `docs/development/io_engine_baseline.md` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | EventLoop's owner thread performs registration, cancellation, accepted completion commit, wait, phase transition, and callback dispatch. `wakeup()` remains the only direct cross-thread Engine operation. Engine borrows readiness Channels; a successful completion submission acquires the supplied lease until its real terminal packet is dequeued. Quiescing rejects new external admission while already-accepted cleanup and final drain remain legal. EventLoop contains callback exceptions and generation-checks retained notices before invocation. |
| Public surface | `none`; stable installed declarations and targets have zero same-line diff. The IOCP batch constructor is classified `platform_internal`; the new Engine vocabulary and accessor remain source-private. `tests/api/test_public_api_manifest.py` and the blocking same-line comparator passed. |
| Changed production paths | `src/core/net/detail/IoEngine.h`; `src/core/net/detail/PollerIoEngineAdapter.cc`; `src/core/net/detail/IocpPollerAccess.h`; `src/core/net/detail/EventLoopControlRegistry.h`; `src/core/net/EventLoop.cc`; `include/gamenet/core/net/poller/IocpPoller.h`; `src/core/net/poller/IocpPoller.cc` |
| Focused contracts | Exact clean commit, `contract.io_engine.test_io_engine_poller_adapter`: Windows 1/1 in 0.02 s; Linux/WSL 1/1 in 0.03 s. The contract covers owner-thread rejection, invalid/unsupported results, admission seal, actual IOCP terminal drain, callback-local close, stale generation, contained exception, exact budget continuation metrics, option mapping, and cross-thread wakeup. |
| Windows full gate | Exact clean commit; Release build passed; 122/122 CTest tests passed in 12.79 s with four workers. |
| Linux full gate | Exact clean commit; GCC 13.3 Release build passed under WSL Ubuntu 24.04; 122/122 CTest tests passed in 12.66 s with four workers. |
| Benchmark/capacity | Exact clean commit against `669ebb0`, one warmup per side plus three alternating echo pairs. Windows throughput 58.990 -> 61.452 MiB/s (+4.17%), P99 69.3 -> 62.0 us (-10.53%), stop 0.185 -> 0.190 ms (+0.005 ms). Linux throughput 49.991 -> 49.593 MiB/s (-0.80%), P99 89.948 -> 91.191 us (+1.38%), close 1.262 -> 1.238 ms. Linux stop 0.122 -> 0.160 ms (+0.038 ms, +30.9%) crossed the percentage investigation trigger, but all paired samples were drained in 0.12–0.18 ms; the sub-0.04 ms absolute shift is accepted as local scheduler/timer noise. No connection storage or per-connection allocation changed; the decision is integrate. |
| Sanitizer/race evidence | No sanitizer result is claimed. This slice adds no shared producer queue or non-owner mutation path; completion storage still uses the already-contracted native backend lease. Sanitizer/race evidence remains mandatory for IOE-C1's direct completion storage/retirement change. |
| Review | Codex `/root` self-review followed intent, public contract, invariant, thread-affinity, ownership, lifecycle, implementation, and test-completeness order. Production no longer includes a repository test harness for IOCP progress; `IocpPollerAccess` is the source-private accessor. Independent ARCH-G1 review remains pending and is not claimed as closure evidence. |
| Governance | Exact clean commit; all 26 repository/API/CI guard scripts passed; 32 active intents and 156 explicit verification paths; same-line stable public API blocking gate passed. |
| Remote evidence | `not collected`; this is local exact-commit integration evidence, not CI, endurance, capacity, or release evidence. |
| Decision | `integrate` and close IOE-R1. Move the active implementation front directly to IOE-R2 generation-safe epoll Readiness contracts; no candidate freeze is introduced. |

## IOE-R2 Generation-Safe epoll Readiness Engine — 2026-08-19

| Field | Evidence |
| --- | --- |
| Slice | IOE-R2 closure: source-private ReadinessPort vocabulary, generation-safe epoll registration/notices, internal backend wakeup, bounded level-triggered wait, stale/current-interest filtering, and legacy EPollPoller compatibility shell |
| Commit | `6f45aa6e78152b8fd86df925962e580101b2f2ee` |
| Parent/baseline | Parent `d7b8ad8ac66a86625b9fed3c552bd131fe96f40a`; runtime and performance baseline `669ebb0a7c5c475dea74b12275c66a2ce1876804`; `docs/development/io_engine_baseline.md` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `intents/modules/platform_runtime.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | EventLoop's owner thread exclusively registers, updates, cancels, waits, resolves tokens, publishes borrowed Channel targets, and releases the port. `wakeup()` is the sole cross-thread port method and carries no callback/business data. Epoll payloads contain a packed source/generation token, never a raw Channel pointer. Remove/re-register changes generation; dispatch validates current source, generation, target, and interests before exposing a notice. The port owns epoll, eventfd, fixed batches, and its one registration map; EventLoop owns Channels and contains re-entrant callback removal through its active-batch generation contract. |
| Public surface | `none`; `ReadinessPort`, `EpollReadinessPort`, the Engine option/progress fields, and the deterministic decoder seam are source-private. `EPollPoller` is `platform_internal`. `tests/api/test_public_api_manifest.py` and the blocking same-line comparator report zero stable-header/target drift. |
| Changed production paths | `src/core/net/detail/ReadinessPort.h`; `src/core/net/detail/EpollReadinessPort.h`; `src/core/net/detail/EpollReadinessPort.cc`; `src/core/net/detail/IoEngine.h`; `src/core/net/detail/PollerIoEngineAdapter.cc`; `src/core/net/poller/EPollPoller.cc`; `include/gamenet/core/net/poller/EPollPoller.h`; `src/core/net/EventLoop.cc`; `src/core/CMakeLists.txt` |
| Focused contracts | Exact clean commit: `contract.io_engine.test_readiness_engine` passed 1/1 on Windows in 0.03 s and Linux in 0.00 s. It proves owner-only mutation, typed invalid/conflict results, nonzero identity, stable update/disable generation, remove/re-register generation replacement, stale-token rejection, current-interest filtering, same-identity mask merge, fixed wait continuation, internal cross-thread wakeup, level-trigger repetition, application-owned payload, and final EAGAIN. |
| Windows full gate | Exact clean commit; Release build passed; 123/123 CTest tests passed in 12.92 s with four workers (8 unit, 102 contract, 13 integration; 96 threading, 101 lifecycle). |
| Linux full gate | Exact clean commit; GCC 13.3 Release build passed under WSL Ubuntu; 123/123 CTest tests passed in 12.95 s with four workers (8 unit, 102 contract, 13 integration; 96 threading, 101 lifecycle). |
| Benchmark/capacity | Exact clean commit against `669ebb0`, one warmup per side/scenario plus three alternating pairs. Windows echo throughput 60.447 -> 63.270 MiB/s (+4.67%), P99 71.3 -> 60.9 us (-14.59%), close 1.469 -> 1.718 ms (+0.249 ms), stop 0.167 -> 0.193 ms (+0.026 ms); 256-connection memory 11,456 -> 11,712 B/connection (+2.23%), establish 14.955 -> 14.357 ms (-4.00%), close 5.862 -> 4.940 ms (-15.72%), stop 0.264 -> 0.325 ms (+0.061 ms). Linux echo throughput 37.649 -> 38.913 MiB/s (+3.36%), P99 156.325 -> 162.825 us (+4.16%), close 1.362 -> 1.388 ms, stop 0.177 -> 0.139 ms; 256-connection memory 5,248 -> 5,248 B/connection (0.00%), establish 96.610 -> 87.738 ms (-9.18%), close 3.178 -> 2.925 ms (-7.97%), stop 0.335 -> 0.192 ms. All samples drained. Linux hot-path and memory changes stay within the 5% investigation threshold. Windows sub-millisecond close/stop percentage changes crossed the percentage trigger but not an absolute 0.25 ms; paired ranges overlap prior local scheduler noise, so no runtime regression is accepted. Decision: integrate. |
| Sanitizer/race evidence | Clean detached checkout of exact commit: Linux ASan/UBSan with leak detection and halt-on-error passed both IO Engine contracts 2/2 in 0.08 s; Linux TSan halt-on-error passed the same 2/2 in 0.07 s. No sanitizer or race finding. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review found and fixed a queued native-mask edge: readiness is now intersected with current interests while error/close remain unconditional. It also preserved the Linux production adapter's single registration map and left the legacy Poller map only in the compatibility shell. Independent ARCH-G1 review remains pending and is not claimed here. |
| Governance | Exact clean commit; all 33 repository/API/CI guard scripts passed; 32 active intents and 157 explicit verification paths; the same-line stable public API blocking gate passed with no changes. CI/long-soak inventory gates were intentionally advanced to 123 total and 96 threading tests. |
| Remote evidence | `not collected`; this is local exact-commit integration evidence, not remote CI, endurance, or release evidence. |
| Decision | `integrate` and close IOE-R2. Move the active Engine front directly to IOE-C1 native Completion notices and operation obligations; no candidate freeze is introduced. |

## IOE-C1 Direct Accept/Connect Completion Consumers — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | IOE-C1 direct-consumer closure: production EventLoop dispatches typed Accept, Connect, Read, and Write completions under one owner budget without publishing fake Channel readiness |
| Commit | `d0f2e07c3e3798605ac5d60ee33a7f7689fd542c` |
| Parent/baseline | Parent `82d89831de81522ecd25c8eedd42f395e2a07613`; runtime and performance baseline remains `docs/development/io_engine_baseline.md` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `intents/modules/acceptor.intent.md`; `intents/modules/connector.intent.md`; `intents/modules/event_loop.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md` |
| Owner/lifetime | The EventLoop owner thread alone submits, dequeues, validates, consumes, replenishes, cancels, and retires AcceptEx/ConnectEx operations. Fixed accept-slot state and one connect-attempt state are retained through the native terminal packet. Accept replenishes the exact slot before invoking the re-entrant upper callback. Connector uses a weak owner observer; stale or revoked consumers perform only leased-state cleanup. Cross-thread stop/cancel remains marshaled through EventLoop wakeup and queued owner-thread work. |
| Public surface | `none`; no stable installed declaration or target changed. `IocpPoller` remains `platform_internal`; the same-line v0.3 manifest comparator and public API manifest gate passed. |
| Changed production paths | `include/gamenet/core/net/poller/IocpPoller.h`; `src/core/net/Acceptor.cc`; `src/core/net/Connector.cc`; `src/core/net/detail/IocpPollerAccess.h`; `src/core/net/detail/PollerIoEngineAdapter.cc`; `src/core/net/poller/IocpPoller.cc` |
| Focused contracts | Exact clean commit, Windows Release: `ctest --test-dir build -C Release -R "^(contract\\.io_engine\\.test_completion_engine|contract\\.acceptor\\.test_acceptor_iocp_pool|contract\\.connector\\.test_connector_thread_contract|integration\\.tcp\\.test_iocp_accept_connect_quit_completion_drain)$" --output-on-failure`; 4/4 passed in 12.78 s. The Connector contract also passed 10/10 alone and 10/10 in parallel with the accept-pool and real quit/drain integration contracts before commit. |
| Windows full gate | Same immutable file tree: Debug build and 124/124 CTest passed in 14.28 s; Release build and 124/124 CTest passed in 15.74 s. |
| Linux full gate | Same immutable file tree under WSL/GCC 13.3 with ASan/UBSan, leak detection, and halt-on-error: build passed and 124/124 CTest passed in 14.22 s. |
| Benchmark/capacity | Windows Release same-tree smoke, five samples. Echo median: 118,754.620 messages/RTT/s, 57.9857 MiB/s, P99 70.3 us, close 1.8204 ms, stop 0.2322 ms; throughput was -3.56% versus the immediately preceding local median and remained within the 5% investigation threshold. The 256-connection median was 13.6504 ms establishment, 18,754.029 connections/s, 12,256 B/connection, 7.7469 ms close, and 0.2554 ms stop. This is integration smoke, not promotion evidence. |
| Sanitizer/race evidence | Linux ASan/UBSan full gate passed 124/124 with leak detection and halt-on-error. No TSan result is claimed for Windows-overlapped operation code; repeated ownership/lifecycle contracts provide the local race-stability evidence. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review preserved legacy `IocpPoller::poll()` publication only as an isolated compatibility shell and verified that production access cannot call `publishCompletionNotices`. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Same immutable file tree: all 35 repository/API/CI guards passed; `git diff --check` passed; the required-baseline public manifest verification and blocking v0.3 same-line comparator passed with zero stable-surface drift. |
| Remote evidence | Direct push verified `refs/heads/main` at `d0f2e07c3e3798605ac5d60ee33a7f7689fd542c`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` IOE-C1 direct consumers. Advance immediately to shutdown convergence and removal of the legacy completion-publication/Channel-mailbox compatibility shell; no freeze is introduced. |

## IOE-C1 Compatibility Retirement and Shutdown Closure — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | IOE-C1 closure: retire completion-to-Channel fake-readiness publication and make `Running -> Quiescing -> FinalDraining -> Shutdown` observably monotonic |
| Commit | `c2d7e9d65dab8b110b58c20594b0cebd5199cf26` |
| Parent/baseline | Parent `152fdc0013ebcd27215c943c7ae37324d9b6785e`; direct-consumer checkpoint and local performance comparison `d0f2e07c3e3798605ac5d60ee33a7f7689fd542c`; `docs/development/io_engine_baseline.md` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `intents/modules/acceptor.intent.md`; `intents/modules/event_loop.intent.md`; `intents/modules/platform_runtime.intent.md`; `intents/modules/poller.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md` |
| Owner/lifetime | The EventLoop owner thread exclusively waits, directly invokes each typed completion consumer, advances shutdown phase, and retires terminal operation storage. Acceptor slots and connection drivers retain accepted kernel obligations until their real terminal packets. Observer revocation cannot strand storage cleanup. Re-entrant `quit()` during FinalDraining cannot roll phase backward. Cross-thread quit/cancel is marshaled through EventLoop control work and wakeup. |
| Public surface | `none`; no stable installed declaration or target changed. `Channel.h` remains byte-identical to the reviewed v0.3 stable fingerprint. Its three old private IOCP pointer slots remain unreachable, uninitialized layout bytes until an explicitly reviewed breaking line. The same-line blocking API comparator passed. |
| Changed production paths | `include/gamenet/core/net/platform/IocpOperation.h`; `include/gamenet/core/net/poller/IocpPoller.h`; `src/core/CMakeLists.txt`; `src/core/net/Acceptor.cc`; `src/core/net/Channel.cc`; `src/core/net/EventLoop.cc`; `src/core/net/detail/EventLoopIocpAssociationHarness.h`; `src/core/net/detail/EventLoopLifecycleRegistry.h`; `src/core/net/detail/IocpOperationState.h`; `src/core/net/poller/IocpPoller.cc` |
| Focused contracts | Exact clean commit, Windows Release: `ctest --test-dir build -C Release -R "(event_loop_lifecycle_hub\|completion_engine\|poller_contract\|iocp_quit_completion_drain\|iocp_accept_connect_quit_completion_drain)$" --output-on-failure`; 5/5 passed in 0.15 s. Before commit, the six most affected contracts repeated 20 times without failure. |
| Windows full gate | Same immutable file tree: Debug build and 124/124 CTest passed in 14.24 s; Release build and 124/124 CTest passed in 12.94 s. Real cancelled read completion was consumed during Quiescing, and re-entrant FinalDraining quit remained monotonic. |
| Linux full gate | Same immutable file tree under WSL Ubuntu 24.04/GCC with ASan/UBSan, leak detection, and halt-on-error: build passed and 124/124 CTest passed in 14.09 s. |
| Benchmark/capacity | Windows Release, five serialized samples per scenario. Echo median: 124,021.278 RTT/s, 60.5573 MiB/s, P99 68.1 us, close 1.3707 ms, stop 0.1634 ms; throughput is +4.43% versus `d0f2e07`. The 256-connection median is 15.7157 ms establishment, 16,289.443 connections/s, 12,208 B/connection, 6.8434 ms close, and 0.2995 ms stop. Connection-establishment throughput is -13.15%, while close latency and retained bytes improve; this directional smoke has no connection-scenario regression threshold and is not paired promotion evidence. All ten runs report `status: ok`; integrate. |
| Sanitizer/race evidence | Linux ASan/UBSan full gate passed 124/124 with leak detection and halt-on-error. No TSan result is claimed for Windows-overlapped code; focused completion/lifecycle contracts repeated 20 times and the exact-commit critical set passed. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review found and fixed shutdown phase rollback on re-entrant quit and an obsolete lifecycle guard that still required fake readiness. Stable `Channel.h` was deliberately preserved. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Same immutable file tree: all 35 repository/API/CI guard scripts passed; `git diff --check` passed; 32 active intents and 160 explicit verification paths passed; required-baseline manifest verification and the blocking v0.3 same-line comparator passed with zero stable-surface drift. |
| Remote evidence | Direct ordinary push verified `refs/heads/main` at `c2d7e9d65dab8b110b58c20594b0cebd5199cf26`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close IOE-C1. Advance immediately to RTM-R1 Profile A (`SingleLoopInlineEvent`) contracts and its first runnable vertical slice; no freeze is introduced. |

## RTM-R1 Profile A SingleLoopInlineEvent — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | First TCP-only Runtime Profile: one caller-owned EventLoop, zero TcpServer worker loops, per-connection framing, bounded inline handler dispatch, same-owner queued continuation, explicit overload/violation closure, and runnable framed echo |
| Commit | `adb8b483d9b00ed0e9723321f2d7438e43a5e478` |
| Parent/baseline | Parent `c27ab69984006c385d39f3f42549e5b130b536ee`; architecture baseline `docs/development/io_engine_baseline.md`; Profile A zero-worker echo sample recorded below |
| Intent/ADR | `intents/architecture/runtime_models.intent.md`; `intents/usecases/single_loop_inline_event_profile.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | The caller owns EventLoop and Profile and invokes lifecycle methods on that owner thread. Each TcpConnection context owns its finite PacketFramer/endpoint/continuation state. Handler re-entry holds no lock; admission is rechecked after callback return. A shared callback gate is revoked before shutdown, queued continuation retains only finite connection state and rechecks the gate, and TcpServer owns bounded connection drain. No LogicLoop or cross-domain marshal exists. |
| Public surface | `none`; the support library, header, and echo executable live under `examples/runtime_profiles`, are not installed/exported, and add no stable target/header. Required-baseline manifest verification and the blocking same-line comparator passed. |
| Changed implementation paths | `examples/runtime_profiles/SingleLoopInlineEvent.h`; `examples/runtime_profiles/SingleLoopInlineEvent.cc`; `examples/runtime_profiles/single_loop_inline_echo.cpp`; `examples/CMakeLists.txt`; `tests/contract/runtime_model/test_network_only_profile.cpp`; `tests/CMakeLists.txt` |
| Red-to-green contract | Before implementation, `tests/cmake/test_runtime_profile_contract.py` failed on the absent source and the compiled C++ contract failed to link seven Profile methods. After implementation, the real TCP contract proves 2/2/1 processing of five ordered frames, zero workers/handoffs, continuation queue saturation, handler overrun, output overload, callback-reentrant stop, and bounded shutdown. |
| Focused contracts | Before commit, Windows Debug repeated `contract.runtime_model.test_network_only_profile` 20/20 without failure. Exact clean commit: Windows Release 1/1 in 0.07 s and Linux ASan/UBSan 1/1 in 0.15 s; the static Profile guard and required-baseline public manifest verification also passed. |
| Windows full gate | Same immutable file tree: Debug build and 125/125 CTest passed in 13.11 s; Release build and 125/125 CTest passed in 12.98 s (8 unit, 104 contract, 13 integration; 98 threading, 103 lifecycle). |
| Linux full gate | Same immutable file tree under WSL Ubuntu 24.04/GCC with ASan/UBSan, leak detection, and halt-on-error: build passed and 125/125 CTest passed in 14.12 s. |
| Benchmark/capacity | Windows Release, five serialized zero-worker Core echo samples with 4 connections, 10,000 messages/connection, 256-byte payload, 500 ms settle, 30 s timeout. Median: 122,760.126 RTT/s, 59.9415 MiB/s, P99 63.6 us, close 0.1748 ms, stop 0.1297 ms, working-set delta 184,320 bytes. This is the minimum-handoff topology baseline, not paired promotion evidence; the framed Profile contract proves the cross-domain handoff count remains zero. |
| Sanitizer/race evidence | Linux ASan/UBSan full gate passed 125/125 with leak detection and halt-on-error. No TSan result is claimed: Profile A adds no shared producer queue or non-owner mutation path, and owner/callback contracts repeated 20 times locally. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review kept the composition non-installed, prohibited recursive inline continuation, made queue rejection terminal, and revalidated callback admission after re-entrant stop. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Same immutable file tree: all 35 repository/API/CI guards passed; `git diff --check` passed; 33 active intents and 164 explicit verification paths passed. CI/long-soak inventory gates advanced to 125 total and 98 threading tests. |
| Remote evidence | Direct ordinary push verified `refs/heads/main` at `adb8b483d9b00ed0e9723321f2d7438e43a5e478`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close RTM-R1 Profile A. Advance immediately to Profile B `MultiIoQueuedEvent` bounded handoff contracts; keep Profile A provisional and non-installed, with no freeze. |

## RTM-R1 Profile B MultiIoQueuedEvent — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Second TCP-only Runtime Profile: base accept loop, two TcpServer network-owner workers, one distinct caller-owned logic EventLoop, bounded GameCommandQueue, coalesced producer wake, bounded drain continuation, generation-safe owner return, typed saturation recovery, dual stop futures, synchronized tail/queue metrics, runnable echo, and opt-in directional benchmark |
| Commit | Implementation `4c14911366dd3e1d84d7c5a7170236d422cc6b94`; exact closing checkpoint `633d61315a9e28db643ee91214dc2f26a9b64630` separates logic-side lost-wakeup rearm from producer coalescing metrics |
| Parent/baseline | Parent `00bdb23708bb949fdf2cf30236711eb07f107c1b`; Profile A checkpoint `adb8b483d9b00ed0e9723321f2d7438e43a5e478`; Profile B benchmark composition is 4 clients / 2 network workers / 1 logic owner / 5,000 messages per client / 256-byte payload / 64-message client batches |
| Intent/ADR | `intents/architecture/runtime_models.intent.md`; `intents/usecases/multi_io_queued_event_profile.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | TcpServer selects and retains each established connection's network owner. Per-connection context owns the framer and a generation-revocable route; immutable commands cross to one caller-owned logic EventLoop. The handler receives no endpoint. Output returns only through the captured route owner executor after before/after-handler and owner-callback generation checks. Profile destruction precedes logic-loop stop. |
| Admission/failure | GameCommandQueue command/byte/payload limits, an atomic scheduled-drain gate, `maxCommandsPerDrain`, finite connection buffers, and fixed histograms bound work/memory. Queue or payload rejection closes one route; logic wake/continuation rejection revokes Profile admission, closes/discards and counts backlog, and requests every route close. No business work uses inline fallback or EventLoop control/lifecycle admission. |
| Public surface | `none`; support header/source, echo, and benchmark remain below `examples/` or `benchmarks/`, are not installed/exported, and add no stable target/header. The v0.3 structured public API comparator reports zero stable-surface drift. |
| Red-to-green contract | Before implementation, the static guard failed on absent `MultiIoQueuedEvent.cc`; the compiled real-TCP contract failed on absent `MultiIoQueuedEvent.h`. Green coverage proves two network owners, one blocked logic owner, one producer wake plus nine merges, two-command drains, ordered output, queue-full route revocation and recovery, stale input/output rejection, and active-handler-delayed logic stop. A Windows IOCP accept race in the fixture was exposed by repeats and repaired with an explicit two-owner-ready phase barrier. |
| Focused contracts | Exact closing checkpoint: Windows MSVC Release real-TCP contract passed 20/20 in 0.75 s; Linux/epoll ASan/UBSan passed 20/20 in 0.79 s. Before closure it also passed 20/20 Windows Debug. The exact benchmark then caught producer/rearm metric conflation; `633d613` fixed it and 10/10 Windows Release plus one Linux sanitizer benchmark passed. |
| Windows full gate | Exact closing implementation tree: Debug build and 126/126 CTest passed in 52.69 s; Release build and 126/126 passed in 51.86 s (8 unit, 105 contract, 13 integration; 99 threading, 104 lifecycle). |
| Linux full gate | Exact closing implementation tree under WSL Ubuntu 24.04/GCC with ASan/UBSan, leak detection, and halt-on-error: build passed and 126/126 CTest passed in 53.79 s. |
| Benchmark | Ten exact-checkpoint Windows Release samples all `status: ok`. Median: 362,545.500 messages/s, 88.512 MiB/s, N→L P99/P999 96/128 us, L→N P99/P999 64/128 us, maximum queue age 162 us, queue-depth high-water 127.5, working-set delta 1,656,832 bytes, and shutdown 1.955 ms. Every sample reports 40,000 cross-domain handoffs and `producerWakePosts + producerWakeMerges == 20,000`; directional only, not promotion evidence. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Repeat testing found and fixed early-client IOCP accept timing in the fixture; exact benchmark invariants found and fixed the producer/rearm metric boundary. Profile remains provisional/non-installed. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact implementation tree: all 36 repository/API/CI guards passed; `git diff --check` passed; 34 active intents and 168 explicit verification paths passed. CI/long-soak gates advanced to 126 total and 99 threading tests; structured public API comparison reports no stable-surface drift. |
| Remote evidence | Direct ordinary push verified `refs/heads/main` at `633d61315a9e28db643ee91214dc2f26a9b64630`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close RTM-R1 Profile B. Advance immediately to Profile C `MultiIoDedicatedFixedTick`; keep Profile A/B provisional and non-installed, with no freeze. |

## RTM-R1 Profile C MultiIoDedicatedFixedTick — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Third TCP-only Runtime Profile: base accept loop, two TcpServer network-owner workers, one distinct caller-owned logic/tick EventLoop, authoritative fixed-rate skip or bounded-catch-up cadence, bounded GameCommandQueue drain per tick, generation-safe owner return, typed route-local failure, dual stop futures, runnable echo, and opt-in directional benchmark |
| Commit | Exact implementation checkpoint `da57edc887421503e93ca743afb8a3373642c878` |
| Parent/baseline | Parent `75d21a82fc89e6c144107b34b0dcb611b1f99214` established the active intent and red static contract; Profile B checkpoint `633d61315a9e28db643ee91214dc2f26a9b64630`; benchmark composition is 4 clients / 2 network workers / 1 logic owner / 2,000 messages per client / 256-byte payload / 1 ms cadence / bounded catch-up 1 / 1,024 commands per tick |
| Intent/ADR | `intents/architecture/runtime_models.intent.md`; `intents/usecases/multi_io_dedicated_fixed_tick_profile.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | TcpServer retains each established connection on its selected network owner. Immutable commands cross into one bounded queue without per-command logic wake; only the caller-owned logic EventLoop's fixed-rate timer invokes the handler. Output returns through the captured route owner executor after generation checks before the handler, after it, and on owner callback. Stop revokes admission/routes, discards queued work, drains network ownership, retires cadence on the logic owner, and keeps the logic future pending through any committed tick. |
| Cadence/failure | `FixedRateSkipMissed` requires zero catch-up; `FixedRateBoundedCatchUp` requires a positive finite bound. Both map to TimerQueue fixed rate. Queue/payload rejection, handler exception/overrun, output rejection, and endpoint overload close only the affected route. Timer setup is Profile-terminal. Tick jitter/duration/queue age, catch-up/skip, overrun, queue high-water, all route outcomes, setup/cancel, shutdown drops, and convergence are bounded/observable. |
| Public surface | `none`; support header/source, echo, and benchmark remain below `examples/` or `benchmarks/`, are not installed/exported, and add no stable target/header. The v0.3 compatibility structured public API comparison reports `has_changes=false` with no added/removed target or header. Installed consumers pass 2/2 and contain no Profile C artifact. |
| Red-to-green contract | Before implementation, the compiled real-TCP contract failed on absent `runtime_profiles/MultiIoDedicatedFixedTick.h`, and the static guard failed on the absent implementation. Green coverage proves invalid topology/cadence rejection; no handler before the authoritative tick; two network owners plus one logic owner; deterministic 2/2/1 drain and ordered return; exact handoffs; bounded catch-up then skip after deterministic overrun; queue saturation with stale-generation rejection and fresh-route recovery; and active-handler-delayed logic stop with queued shutdown discard. |
| Focused contracts | Final implementation tree: Windows Debug 20/20, Windows Release 20/20, and Linux/epoll ASan/UBSan 20/20 real-TCP focused repeats passed. The final assertions include timer setup/cancel acceptance, populated histograms, catch-up bounds, logic-future pending state, stale active-tick output, and queued shutdown discard. |
| Windows full gate | Exact implementation tree: Debug build and 127/127 CTest passed in 51.87 s; Release build and 127/127 passed in 51.42 s (8 unit, 106 contract, 13 integration; 100 threading, 105 lifecycle). |
| Linux full gate | Exact implementation tree under WSL Ubuntu 24.04/GCC 13.3 with ASan/UBSan, leak detection, and halt-on-error: build passed and 127/127 CTest passed in 56.07 s. DrvFS emitted only benign sub-three-second clock-skew warnings; no sanitizer or test failure occurred. |
| Benchmark | Ten Windows MSVC Release samples all `status: ok`; medians: 127,956.083 messages/s, 31.2395 MiB/s, tick-jitter P999 1,024 us, tick-duration P999 256 us, queue-age P999 1,024 us, 0 skipped, 29.5 catch-up, queue HWM 256, working-set delta 1,353,728 bytes, shutdown 1.939 ms. Ten WSL Release samples all `status: ok`; medians: 5,643.996 messages/s, 1.378 MiB/s, the same histogram buckets, 0 skipped, 168 catch-up, queue HWM 256, working-set delta 1,366,016 bytes, shutdown 1.3665 ms. Every sample reports exactly 16,000 cross-domain handoffs. The large Windows/WSL throughput difference is directional IOCP-versus-WSL/DrvFS epoll evidence, not a native-platform regression or promotion decision. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. The composition reuses TimerQueue's fixed-rate policy, never schedules per-command logic drains, keeps all transport mutation on the captured network owner, and remains provisional/non-installed. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact implementation tree: all 38 repository/API/CI guards passed; `git diff --check` passed; 35 active intents and 172 explicit verification paths passed. CI/long-soak inventory gates advanced to 127 total and 100 threading tests; the current 0.3 structured public API comparison reports zero stable-surface drift. |
| Remote evidence | Direct ordinary push verified `refs/heads/main` at `da57edc887421503e93ca743afb8a3373642c878`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close RTM-R1 Profile C. Advance immediately to RTM-R2 placement/sharding contracts; keep Profiles A/B/C provisional and non-installed, with no freeze. |

## RTM-R2 Profile D MultiIoShardedHybrid — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Fourth TCP-only Runtime Profile: base accept loop, two TcpServer network-owner workers, two distinct caller-owned logic-cell EventLoops, independent connection placement and stable player/room/scene sharding, per-cell bounded FIFO, event-driven drain plus authoritative fixed-rate tick, cell-local no-overtake ordering, generation-safe owner return, isolated saturation, aggregate stop future, runnable echo, and opt-in directional benchmark |
| Commit | Exact implementation checkpoint `b3b184b1cd8d28a256cffc60679ab832373dcfef` |
| Parent/baseline | Parent `b89a4e52cdb4f9ca5c4c88bdd41220d24b165800`; Profile C checkpoint `da57edc887421503e93ca743afb8a3373642c878`; benchmark composition is 4 clients / 2 network workers / 2 logic cells / 2,000 messages per client / 256-byte payload / 1 ms cadence / 1,024-command event and tick budgets |
| Intent/ADR | `intents/architecture/runtime_models.intent.md`; `intents/usecases/multi_io_sharded_hybrid_profile.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | TcpServer selects and retains each established connection's immutable network owner. Stable hashing of an immutable player/room/scene key selects one of at least two external logic-cell owners independently; one connection may target multiple cells. Commands capture route generation and owner executor. Each cell owns its finite FIFO, sequence, event-drain gate, and fixed-rate timer. Stop revokes routes/admission, discards queued work, waits for committed handlers, retires all timers on their owners, and resolves one aggregate logic future only after every cell converges. |
| Ordering/failure | Each cell assigns a monotonic sequence. Event drain stops at an earlier fixed-tick head; the tick releases the ordered prefix under one total budget, so a later event cannot overtake earlier fixed work. No cross-cell global ordering is promised. Per-cell queue/byte/payload/key bounds reject only the affected route; output is posted only to the captured owner after generation revalidation. Timer setup/post failure is Profile-terminal and observable. |
| Public surface | `none`; support header/source, echo, and benchmark live only below `examples/` or `benchmarks/`, are not installed/exported, and add no stable target/header. Windows Release install plus external consumers passed 2/2, the install manifest contains no Profile D artifact, and the v0.3 public API manifest remains unchanged. |
| Red-to-green contract | Before implementation, the static contract failed on absent `MultiIoShardedHybrid.cc`; the compiled real-TCP contract failed on absent `MultiIoShardedHybrid.h`. Green coverage rejects invalid topology/budgets/router; proves two network owners and two logic cells, independent stable sharding, one transport targeting multiple cells without owner migration, event/fixed no-overtake, cross-cell progress, exact handoffs, isolated cell saturation, stale-generation output rejection, and active-handler-delayed aggregate stop. |
| Focused contracts | Exact implementation tree: Windows Debug and Release real-TCP contract each passed 20/20; Linux/epoll ASan/UBSan with leak detection and halt-on-error passed 20/20. The final contract records exact 10 handoffs in ordering coverage, fixed-head deferral, two retired cell timers, queued shutdown discard, and no owner/order violation. |
| Windows full gate | Exact implementation tree: Debug build and 128/128 CTest passed in 13.22 s; Release build and 128/128 passed in 13.01 s (8 unit, 107 contract, 13 integration; 101 threading, 106 lifecycle). |
| Linux full gate | Exact implementation tree under WSL Ubuntu 24.04/GCC 13.3 with ASan/UBSan, leak detection, and halt-on-error: build and 128/128 CTest passed. DrvFS emitted only benign sub-three-second clock-skew warnings; no sanitizer or test failure occurred. |
| Benchmark | Ten Windows MSVC Release samples all `status: ok`; medians: 120,857.0175 messages/s, 29.506 MiB/s, cell imbalance 1.0, queue-age P999 2,048 us, queue HWM 127, 457 fixed-head deferrals, working-set delta 1,665,024 bytes, profile drain 42 us, shutdown 1.914 ms. Ten WSL Release samples all `status: ok`; medians: 5,626.593 messages/s, 1.374 MiB/s, cell imbalance 1.0, queue-age P999 1,024 us, queue HWM 128, 135.5 deferrals, working-set delta 1,458,176 bytes, profile drain 30.5 us, shutdown 1.382 ms. Every sample reports exactly 16,000 cross-domain handoffs. The Windows/WSL difference is directional IOCP-versus-WSL/DrvFS epoll evidence, not a native-platform regression or promotion decision. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review added overflow-safe combined queue accounting, per-cell committed-batch stop-drop metrics, strict lane parsing, and the fixed-head deferral observation after an event prefix. Profile remains provisional/non-installed. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact implementation tree: all 39 repository/API/CI guards passed; `git diff --check` passed; 36 active intents and 176 explicit verification paths passed. CI/long-soak inventory gates advanced to 128 total and 101 threading tests; the current v0.3 public API manifest reports zero stable-surface drift. |
| Remote evidence | Direct push verified `refs/heads/main` at `b3b184b1cd8d28a256cffc60679ab832373dcfef`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close RTM-R2 Profile D. Advance immediately to IOE-X1 one-shot completion contracts and the cross-Profile common-capability review; keep Profiles A/B/C/D provisional and non-installed, with no freeze. |

## IOE-X1 Experimental io_uring One-Shot Engine — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Linux-only, default-off, non-installed raw-syscall io_uring Completion Engine with finite one-shot Accept/Recv/Send, typed SQ saturation, generation-safe cancel/terminal retirement, bounded storage leases/final drain, and an opt-in structured pipeline benchmark |
| Commit | Real Engine implementation `a39b7022a05cd0ff373334933bf670451d73269e`; exact evidence/benchmark closure checkpoint `d3b31c5c4e7966553094f7e42cf74f1b49a11077` |
| Parent/baseline | Engine implementation parent `59b77bae1c4e0502c97cc93067ce63e3fae17a50`; benchmark parent `a39b7022a05cd0ff373334933bf670451d73269e`; IOE-C1 closure `c2d7e9d65dab8b110b58c20594b0cebd5199cf26`; production Linux remains the epoll IOE-R2 path closed at `6f45aa6e78152b8fd86df925962e580101b2f2ee` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `intents/modules/platform_runtime.intent.md`; `intents/modules/poller.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | One caller EventLoop thread owns all enqueue/flush/wait/cancel/notice/quiesce/shutdown mutation; foreign mutation is rejected. The Engine owns its ring fd, mappings, finite SQEs, slot generations, address/buffer storage, optional lease, cancel bookkeeping, and accepted socket until transfer. ASYNC_CANCEL CQE never retires target storage; the target terminal CQE alone moves the lease into exactly one notice. Quiesce seals admission, flushes staged work, cancels active operations, and drains target plus cancel CQEs before physical Shutdown. No user callback or cross-thread fallback exists. |
| Public surface | `none`; `GameNet::experimental`, its header, contract, and benchmark are Linux-only, default-off, non-installed, and absent from package exports. Windows explicitly rejects `GAMENET_ENABLE_EXPERIMENTAL=ON`; the normal Core build remains experimental-off and epoll/IOCP behavior is unchanged. |
| Changed production paths | `none` in installed Core data paths. New implementation is confined to `src/experimental/io_uring`; root/test/benchmark CMake only admits it behind the Linux experimental option. CI adds isolated Debug and ASan/UBSan contract lanes without changing the default inventory. |
| Red-to-green contract | The static IOE-X1 guard initially failed because `src/experimental/io_uring/CMakeLists.txt` was absent. Green contracts use a real kernel ring and prove invalid/foreign admission, native SQ fill with typed rejection, real Accept/Recv/Send, slot reuse with stale-generation cancellation rejection, repeated-cancel idempotence, lease retention, internal cancel-CQE separation, and final-drain convergence. |
| Focused contracts | Exact closure tree: Linux ASan/UBSan `contract.io_engine.test_io_uring_completion_engine` passed 20/20 with leak detection and halt-on-error. The configured inventory is 129 (8 unit, 108 contract, 13 integration; 102 threading, 107 lifecycle; 1 experimental). Windows experimental configure fails with the required Linux-only diagnostic. |
| Windows full gate | Exact closure tree, experimental-off: MSVC Release build and 128/128 CTest passed (8 unit, 107 contract, 13 integration; 101 threading, 106 lifecycle). This proves the new option/benchmark wiring leaves the supported Windows production surface unchanged; it is not an io_uring runtime claim. |
| Linux full gate | Exact closure tree under WSL Ubuntu 24.04/GCC 13.3 with experimental, ASan/UBSan, leak detection, and halt-on-error: build completed and `LastTest.log` records 129 `Test Passed.` entries, zero failure/timeout/not-run markers, from 04:19 to 04:20 CST. Inventory independently verified 129 with the label counts above. DrvFS emitted only benign clock-skew warnings. |
| Benchmark/capacity | Ten serialized WSL Release samples, each 100,000 round trips / 256-byte payload / depth 64 over real `SOCK_SEQPACKET`, all validated by `tools/validate_io_uring_benchmark.py`. Medians: 866,826.249 round trips/s, 1,733,652.499 operations/s, 423.255 MiB/s, P50/P99/P999 67.5/163.5/247 us, working-set delta 1,028,096 bytes, shutdown 0.0006495 ms. Every sample reports 200,000 accepted = 200,000 terminal, zero SQ rejection, zero fallback, and zero active/ready/owned-byte residue. Five serialized production epoll Core echo references at the same 100,000 messages and 256-byte payload report median 34,047.923 messages/s, 16.625 MiB/s, and P50/P99/P999 24.709/96.127/173.721 us; topology differs (one in-flight TCP RTT versus depth-64 AF_UNIX pipeline), so this pair is directional context, not a backend superiority or regression claim. |
| Sanitizer/race evidence | Linux ASan/UBSan full 129/129 and focused 20/20 pass with leak detection and halt-on-error; one 10,000-round-trip benchmark sample also validates under ASan/UBSan. No TSan result is claimed because the Engine API is owner-thread-only and the benchmark peer shares no Engine state. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review added constructor rollback, overflow-safe byte accounting, generation/repeated-cancel coverage, a 255-op capability probe bound, structured benchmark invariants, and explicit rejection of advanced features. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact closure tree: all 39 repository/API/CI guards passed; `git diff --check` passed; 36 active intents and 178 explicit verification paths passed. The v0.3 structured public API comparator reports zero stable-surface drift. Default CTest remains 128; the isolated experimental inventory is 129. |
| Remote evidence | Direct pushes verified `refs/heads/main` at implementation `a39b7022a05cd0ff373334933bf670451d73269e` and evidence closure `d3b31c5c4e7966553094f7e42cf74f1b49a11077`; no remote CI, endurance, or release result is claimed. |
| Decision | `integrate` and close IOE-X1 as experimental evidence. Keep epoll as production default/fallback and keep all advanced io_uring/production integration deferred. Advance immediately to the cross-Profile real-TCP integration contract and common-capability review; no freeze. |

## Cross-Profile Real-TCP Lifecycle Review — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | One combined integration contract drives the same framed TCP echo and graceful-stop lifecycle through Runtime Profiles A/B/C/D, preserves each Profile's distinct ownership and handoff assertions, closes the common-capability review as `NO-PROMOTION`, and repairs Profile C cadence-stop result publication ordering. |
| Commit | Exact implementation checkpoint `1c4c58f4e53e12dfae8ad63e3c83128d420f2810` |
| Parent/baseline | Parent `e9f6ac0`; Profile checkpoints A `adb8b483`, B `633d613`, C `da57edc`, and D `b3b184b1`; IOE-X1 evidence closure `d3b31c5c4e7966553094f7e42cf74f1b49a11077` |
| Intent/ADR | `intents/architecture/runtime_models.intent.md`; `intents/usecases/multi_io_dedicated_fixed_tick_profile.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `docs/architecture/runtime_profile_common_capability_review.md`; `rules/testing_rules.md` |
| Owner/lifetime | The normalized test vocabulary proves only one echoed command, one retired connection, drained obligations, and safe owner/generation behavior. It separately requires Profile A zero handoff; B/C/D two explicit cross-domain transfers; C cadence retirement; and D two-cell retirement without owner migration or order violation. Profile C now publishes the cadence-stop admission result before its logic future can complete; an owner Tick that wins retirement records the converged path as accepted. |
| Public surface | `none`; all Profile classes, options, handlers, metrics, stop handles, examples, and the normalized observation remain non-installed. No stable target, header, enum, factory, selector, or ABI surface changed. The v0.3 same-line public API comparator reports `has_changes=false` with zero stable fingerprint, target, or header drift. |
| Focused contracts | Same immutable file tree after both fixture and lifecycle fixes: combined A/B/C/D real-TCP integration passed Windows Debug 300/300, Windows Release 300/300, and Linux epoll ASan/UBSan 100/100. The repeats exposed and drove closure of the Profile C result-publication race and Windows nonblocking-client `WSAENOTCONN` transient handling; only post-fix passing evidence is counted. |
| Windows full gate | Same immutable file tree: Debug 129/129 CTest passed in 53.01 s; Release 129/129 passed in 52.32 s. Inventory independently verified 129 tests: 8 unit, 107 contract, 14 integration, 102 threading, 107 lifecycle, and 5 runtime_model. |
| Linux full gate | Same immutable file tree under WSL Ubuntu 24.04/GCC 13.3 with experimental IOE-X1, ASan/UBSan, leak detection, and halt-on-error: 130/130 CTest passed in 55.96 s. Inventory independently verified 130 tests: 8 unit, 108 contract, 14 integration, 103 threading, 108 lifecycle, 5 runtime_model, and 1 experimental. |
| Sanitizer/race evidence | Linux ASan/UBSan full 130/130 and focused 100/100 passed. Cross-platform high-repeat testing directly exercised stop/owner interleavings and found the cadence summary publication race before integration. No TSan result is claimed. |
| Review | Codex `/root` self-review followed intent, public contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. The review rejects a least-common-denominator installed Profile interface because handler context, cadence, placement, stop shape, overload scope, and metrics remain behaviorally distinct. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Same immutable file tree: all 38 repository/API/CI guard scripts passed; `git diff --check`, migration provenance, and scope-boundary verification passed; 36 active intents and 179 explicit verification paths passed. Workflow and long-soak exact inventories advanced to 129 default / 130 experimental and 102 threading tests. |
| Remote evidence | Direct push verified `refs/heads/main` at `1c4c58f4e53e12dfae8ad63e3c83128d420f2810`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` the lifecycle fix and close the common-capability review as `NO-PROMOTION`. Keep Profiles A/B/C/D explicit, provisional, and non-installed; advance immediately to IOE-X2 contract shaping without freezing main. |

## IOE-X2 EventLoop Completion Pump — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Linux-only, default-off, non-installed EventLoop completion pump over the IOE-X1 Engine: ring-fd readiness is only a scheduling trigger; typed terminal notices are consumed under a separate finite per-turn budget; first quit automatically commits participant quiesce/cancel/drain work. |
| Commit | Exact implementation checkpoint `979bc37ef515bfbeb82edb060a1df905175878b8` |
| Parent/baseline | Parent and cross-Profile evidence closure `472beda1f`; IOE-X1 evidence closure `d3b31c5c4e7966553094f7e42cf74f1b49a11077`; production Linux remains the epoll IOE-R2 path. |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `intents/modules/event_loop.intent.md`; `intents/modules/poller.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | One EventLoop owner thread creates, submits, flushes, dequeues, invokes consumers, seals, cancels, drains, retires the Channel, and detaches the lifecycle participant. The Pump borrows the ring completion descriptor through a Channel but never stores completion data in that Channel. Accepted operations retain Engine-owned storage and the supplied lease through their real terminal CQE. Consumer exceptions are contained and counted; they cannot skip target/cancel CQE drain. Foreign mutation is rejected. |
| Public surface | `none`; the Pump, Engine descriptor seam, target, and contracts stay under `src/experimental/io_uring`, are Linux-only/default-off/non-installed, and do not alter a stable header, target, or fingerprint. The v0.3 structured comparator reports `has_changes=false`. |
| Red-to-green contract | Before implementation, the lifecycle contract failed to link on the absent `attachQuiesceParticipant` seam. Green coverage uses real socketpairs to force two ready Recvs through a one-notice Pump budget, re-entrant Send, consumer failure, quit from a terminal consumer, cancellation of a still-pending Recv, lease survival through delivery, and final zero-residue convergence. Invalid options and foreign-thread mutation are also rejected. |
| Focused contracts | Exact implementation tree: Windows lifecycle-hub CTest passed; its notify/quit test contains 100 owner/notifier/quitter races. A mistaken outer 100x repetition later exhausted the Windows loopback dynamic-port pool (`10003` TIME_WAIT sockets) and is excluded as an invalid nested stress model; the exact single run passed after diagnosis. Linux ASan/UBSan `contract.io_engine.test_io_uring_event_loop_pump` passed 100/100 in 3.06 s with leak detection and halt-on-error. |
| Windows full gate | Exact implementation tree, experimental-off: Debug build and 129/129 CTest passed in 79.67 s; Release build and 129/129 passed in 53.94 s. Inventory remains 8 unit, 107 contract, 14 integration, 102 threading, and 107 lifecycle tests. |
| Linux full gate | Exact implementation tree under WSL Ubuntu 24.04/GCC 13.3 with experimental, ASan/UBSan, leak detection, and halt-on-error: 131/131 CTest passed in 56.03 s. Inventory is 8 unit, 109 contract, 14 integration, 104 threading, 109 lifecycle, and 2 experimental tests. |
| Sanitizer/race evidence | Linux ASan/UBSan full 131/131 and Pump 100/100 passed. A separate TSan threading inventory passed 102/102 in 52.29 s; no race, sanitizer, leak, timeout, or residual Engine-state finding occurred. |
| Benchmark/capacity | Release Engine directional smoke on the exact implementation tree completed 100,000 round trips / 256-byte payload / depth 64 in 0.107500475 s: 930,228.448 round trips/s, 454.213 MiB/s, P50/P99/P999 66/109/248 us, 200,000 accepted = 200,000 terminal, zero SQ rejection/fallback, and zero active/ready/owned-byte residue. This bypasses the Pump and therefore proves only that the underlying Engine did not regress; no Pump performance or production-promotion claim is made. |
| Review | Codex `/root` self-review followed intent, contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review separated CQ batch capacity from per-turn consumer budget, made first quit commit participant work before silence can be observed, preserved repeated-quit monotonicity and active self-signal continuation, and kept `TcpConnection` plus advanced io_uring features out of scope. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact implementation tree: all 38 repository/API/CI guards passed; `git diff --check`, migration provenance, and scope-boundary verification passed; 36 active intents and 180 explicit verification paths passed. Default inventory remains 129; isolated experimental inventory advances to 131. |
| Remote evidence | Direct push verified `refs/heads/main` at `979bc37ef515bfbeb82edb060a1df905175878b8`; no remote CI, endurance, capacity, or release result is claimed. |
| Decision | `integrate` and close IOE-X2 as experimental lifecycle evidence. Advance immediately to IOE-X3 contract shaping for one completion-driven connection: explicit socket/operation owner, one Recv in flight, finite Send bytes, pause-without-repost, and close/cancel/terminal retirement; retain epoll as production default and introduce no freeze. |

## IOE-X3 Experimental Single-Connection TCP Driver — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Linux-only, default-off, non-installed `IoUringTcpConnectionDriver` for one already-established real TCP socket: one owner, one Recv and one Send identity, bounded FIFO send bytes/segments, pause/cancel/no-repost, callback re-entry, first-close-reason, and socket release after physical Pump stop. |
| Commit | Exact implementation checkpoint `a5a93e06a15a8cdab0eea5c4f5f55377c12d0694` |
| Parent/baseline | Parent and IOE-X2 evidence closure `0bf8ffe360d28b8e49f0d58a9f7c623fdc43b27c`; IOE-X2 implementation `979bc37ef515bfbeb82edb060a1df905175878b8`; IOE-X1 Engine closure `d3b31c5c4e7966553094f7e42cf74f1b49a11077` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | The constructing EventLoop owner exclusively starts, sends, pauses, resumes, closes, consumes terminal notices, and destroys the Driver. Construction transfers unique ownership of the connected socket. The Driver owns its Pump, active identities, copied bounded FIFO, first close reason, metrics, and stop promise; the caller retains the Driver through every consumer frame. Pump physical stop first drains Engine obligations, removes the ring-fd Channel, and detaches lifecycle state; only its stopped hook closes the socket and publishes Driver stop. |
| Read/write contract | `start()` and resume post at most one Recv. Terminal identity is cleared before the message consumer, and repost revalidates phase/read desire after re-entry. Pause requests cancellation when a Recv is active and otherwise suppresses repost; resume during cancellation waits for the target terminal CQE. Send admission checks segment count before overflow-safe byte capacity, owns a copy only on `Accepted`, keeps one physical Send active, chunks the FIFO front, and accounts every accepted byte as sent or terminally discarded. |
| Public surface | `none`; Driver/Pump additions remain under `src/experimental/io_uring`, Linux-only/default-off/non-installed, and absent from production `TcpConnection`, Poller, package exports, and backend selectors. Exact structured v0.3 comparison reports `has_changes=false`, no compatibility decision, and no stable-surface review. |
| Red-to-green contract | The IOE-X3 static guard first failed on the absent `IoUringTcpConnectionDriver.h`. The green real loopback-TCP contract proves invalid-construction fd rollback, byte/segment rejection, 3-byte physical Send chunking with FIFO output, re-entrant pause/no-repost/resume, resume waiting for cancellation terminal, external EventLoop quit, explicit-close sent-or-discarded accounting, callback failure containment, foreign-thread rejection, one close consumer, and zero residual Engine state. |
| Focused contracts | Exact commit: ASan/UBSan `test_io_uring_event_loop_pump` plus `test_io_uring_tcp_connection_driver` passed 100/100 each in 12.38 s; TSan passed the same two contracts 100/100 each in 9.02 s. The Pump contract additionally observes its stopped hook only after lifecycle detach and zero Engine residue. |
| Windows full gate | Exact commit, experimental-off: Debug 129/129 CTest passed in 55.00 s; Release 129/129 passed in 66.60 s. The default inventory remains 8 unit, 107 contract, 14 integration, 102 threading, and 107 lifecycle tests. |
| Linux full gate | Exact commit under WSL Ubuntu 24.04/GCC 13.3 with experimental, ASan/UBSan, leak detection, and halt-on-error: 132/132 CTest passed in 69.41 s. Inventory is 8 unit, 110 contract, 14 integration, 105 threading, 110 lifecycle, and 3 experimental tests. |
| Sanitizer/race evidence | Exact commit TSan threading inventory passed 105/105 in 52.59 s, including all three experimental contracts. Exact ASan/UBSan full and focused repetitions reported no race, sanitizer, leak, timeout, callback-depth, byte-accounting, or residual-state finding. |
| Benchmark/capacity | Exact commit Release Engine smoke completed 100,000 round trips / 256-byte payload / depth 64 in 0.111071585 s: 900,320.275 round trips/s, 439.610 MiB/s, P50/P99/P999 67/162/207 us, 200,000 accepted = 200,000 terminal, zero SQ rejection/fallback, and zero active/ready/owned-byte residue. It bypasses the Driver and therefore asserts only no underlying Engine regression; IOE-X3 makes no Driver performance or production-topology claim. |
| Review | Codex `/root` self-review followed intent, contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review added the Pump stopped-hook ordering proof, rollback without false accepted-byte peaks, explicit-close sent-or-discarded reconciliation, real TCP rather than AF_UNIX coverage, and an explicit caller-retains-through-callback rule. One Pump/ring per connection is recorded as a proof topology only. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact commit: all 39 repository/API/CI guard scripts passed; `git diff --check`, migration provenance, and scope-boundary verification passed; 36 active intents and 181 explicit verification paths passed. Default inventory remains 129; isolated experimental inventory advances to 132. |
| Remote evidence | Direct push verified `refs/heads/main` at `a5a93e06a15a8cdab0eea5c4f5f55377c12d0694`; no remote CI, endurance, capacity, release, or Driver benchmark result is claimed. |
| Decision | `integrate` and close IOE-X3 as experimental single-connection evidence. Advance immediately to IOE-X4 shared-Pump routing contracts: at least two generation-safe connection routes, per-connection budget isolation, one connection closing without cancelling its neighbor, and owner-quit aggregate drain. Do not promote the per-connection ring topology or modify production `TcpConnection`. |

## IOE-X4 Experimental Shared-Pump TCP Hub — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Linux-only, default-off, non-installed `IoUringTcpConnectionHub`: one EventLoop/Pump/Engine owns a fixed-capacity connection table, generation-routes multiple real TCP Recv/Send operations, separates per-route and aggregate send admission, retires one connection without quiescing its neighbor, and aggregates owner-quit final drain. |
| Commit | Exact implementation checkpoint `3cb917bebf1a33b2d8c0c2afd67554219f2af378` |
| Parent/baseline | Parent and IOE-X3 evidence closure `7bc7228e3cab06f81b3415ead14b2a23f2953539`; IOE-X3 implementation `a5a93e06a15a8cdab0eea5c4f5f55377c12d0694`; IOE-X2 implementation `979bc37ef515bfbeb82edb060a1df905175878b8`; IOE-X1 Engine closure `d3b31c5c4e7966553094f7e42cf74f1b49a11077` |
| Intent/ADR | `intents/architecture/io_engine.intent.md`; `docs/architecture/io_engine_runtime_model_adr.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md` |
| Owner/lifetime | One EventLoop owner exclusively admits routes, submits/cancels operations, mutates connection and operation-route tables, invokes callbacks, and stops the Hub. Each route owns its transferred socket, callbacks, one Recv/Send identity, copied FIFO, first close reason, metrics, and future. Local close cancels only that route and releases its slot after both target terminals; Hub/EventLoop stop retains all remaining routes through shared Pump physical shutdown, publishes every connection future, detaches maintenance, and only then publishes Hub stop. Foreign mutation is rejected before lookup/admission. |
| Generation invariant | Connection identity is slot plus nonzero generation; replacement increments generation before close-callback re-entry can add C into A's retired slot. The Engine now also reserves an operation slot while its terminal notice is decoded but not yet taken, so a newer operation generation cannot overwrite the Hub's fixed route entry while Pump dispatch is budgeted. Direct Engine and A/B/C Hub contracts prove the old generation cannot send, close, route a notice, or hit replacement callbacks. |
| Admission and isolation | Each route enforces finite bytes and segment count before owning a payload copy; a separate overflow-safe Hub byte budget bounds the aggregate. Every accepted byte becomes sent or discarded. Saturating A returns exact connection byte/segment results, saturating the aggregate returns `HubByteLimit`, B still sends and receives after A retires, and all route/Hub byte counters converge to zero. Pause/resume preserves one Recv; throwing message code is contained as `CallbackFailed`. |
| Public surface | `none`; Hub/Engine changes remain under `src/experimental/io_uring`, Linux-only/default-off/non-installed, absent from production `TcpConnection`, Poller, selectors, and package exports. The exact v0.3 same-line comparator reports `has_changes=false`, no compatibility decision, and no stable-surface review. |
| Red-to-green contract | After intent/rules landed locally, experimental CMake generation failed precisely because `IoUringTcpConnectionHub.cc` did not exist. Green coverage uses three real loopback TCP pairs across two simultaneous route slots: A closes, B progresses afterward, A's slot is reused by new-generation C from the close callback, stale A calls reject, C callback failure is contained, and explicit Hub stop drains zero residue. A second scenario rejects capacity with fd rollback, rejects foreign mutation, then quits EventLoop with two pending Recvs and accepted Sends and publishes both `EventLoopQuiescing` futures before Hub stop. |
| Focused contracts | Exact commit: the four IOE-X1/X2/X3/X4 contracts passed 4/4. ASan/UBSan repeated the direct Engine slot-retention and shared-Hub contracts 100/100 each (200 passes total) in 7.68 s; TSan repeated both 100/100 each in 4.42 s. A separate non-sanitized Hub repeat passed 100/100 before commit; only the exact-commit sanitizer repeats are treated as closure evidence. |
| Windows full gate | Exact commit, experimental-off: MSVC Debug 129/129 passed in 52.89 s and Release 129/129 passed in 52.38 s. The default inventory remains 8 unit, 107 contract, 14 integration, 102 threading, and 107 lifecycle tests. |
| Linux full gate | Exact commit under WSL Ubuntu 24.04/GCC 13.3 with experimental, ASan/UBSan, leak detection, and halt-on-error: 133/133 passed in 55.38 s. Inventory is 8 unit, 111 contract, 14 integration, 106 threading, 111 lifecycle, and 4 experimental tests. |
| Sanitizer/race evidence | Exact commit TSan threading inventory passed 106/106 in 52.27 s, including all four experimental contracts. Exact ASan/UBSan full and focused repetitions reported no race, sanitizer, leak, timeout, callback-depth, generation-routing, byte-accounting, fd-ownership, or residual-state finding. |
| Benchmark/capacity | No Hub performance, 256-route capacity, endurance, or production comparison result is claimed in IOE-X4. The slice proves topology and lifecycle correctness at two simultaneous routes; IOE-X5 is explicitly assigned the fixed-256-route burst/churn/soak and Release latency/throughput/memory decision before any production adapter promotion. |
| Review | Codex `/root` self-review followed intent, contract, invariants, thread affinity, ownership, lifecycle, implementation, and test completeness. Review found and fixed Engine operation-slot reuse while an older terminal notice was decoded but undispatched, added direct slot-reservation coverage, exercised Hub pause/resume plus callback failure, and preserved one shared Pump with no per-connection ring/Channel/worker. Independent ARCH-G1 review remains pending and is not claimed. |
| Governance | Exact commit: all 39 repository/API/CI guard scripts passed; `git diff --check`, migration provenance, and scope-boundary verification passed; 36 active intents and 182 explicit verification paths passed. Default inventory remains 129; isolated experimental inventory advances to 133. |
| Remote evidence | Direct push verified `refs/heads/main` at `3cb917bebf1a33b2d8c0c2afd67554219f2af378`; no remote CI, endurance, capacity, release, or Hub benchmark result is claimed. |
| Decision | `integrate` and close IOE-X4 as experimental shared-Pump topology/lifecycle evidence. Advance immediately to IOE-X5 fixed-256-route capacity/soak and directional performance decision; keep epoll as production default and do not add a public selector or modify production `TcpConnection` before that decision. |

## IOE-X5 Shared-Hub Capacity and Directional Decision — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | One shared EventLoop/Pump/Engine simultaneously owns 256 real TCP routes, closes and generation-replaces 64, preserves post-churn progress for 192 retained routes, saturates and recovers the aggregate byte budget, and reconciles all 320 route lifetimes. A separate non-CTest Release benchmark emits validated shared-Hub latency, throughput, memory, shutdown, high-water, rejection, and zero-residue JSON. |
| Commit | Exact implementation checkpoint `d400a9550b2a57e47fef319625dfadc163e6c21c` |
| Intent/rules | `intents/architecture/io_engine.intent.md`; `rules/thread_affinity_rules.md`; `rules/ownership_rules.md`; `rules/testing_rules.md`; `tests/contract/io_engine/test_io_uring_tcp_connection_hub_capacity.cpp` |
| Focused contracts | The implementation tree passed the 256-route capacity/churn contract 20/20 normally, 20/20 under ASan/UBSan with leak detection and halt-on-error, and 20/20 under TSan. It asserts 320 accepted/retired routes and socket closes, 256 active high-water, 512 accepted/sent bytes, one exact Hub-byte rejection, 128 stale-identity rejections, and zero operation/byte residue. |
| Benchmark | Five validated WSL Ubuntu 24.04/GCC 13.3 Release shared-Hub samples at 256 connections, 100 round trips/connection, and 64-byte payload produced medians of 218,581.183 RTT/s, 26.682 MiB/s, P50/P99/P999 1,163/1,270/1,354 us, 3,552 active bytes/connection, and 0.866 ms shutdown. Five production epoll Core samples at matching counts produced 99,316.691 RTT/s, 12.124 MiB/s, P50/P99/P999 2,467/4,447/5,682 us, 5,600 bytes/connection, 1.652 ms connection close, and 0.102 ms server shutdown. Topologies differ, so this is directional and not a superiority claim. |
| Public surface | `none`; capacity and benchmark additions remain Linux-only/default-off/non-installed. Production epoll remains default, no selector or production `TcpConnection` changed, and the same-line v0.3 comparator reported no stable drift. |
| Governance/limit | All 37 standalone repository guards plus migration provenance and API comparison passed; inventory advanced to 134 with 5 experimental tests and 183 intent verification paths. No independent full exact-commit sanitizer/Windows rerun is claimed for IOE-X5; the immediately following IOE-X6 exact gates supersede the evolved Hub tree. |
| Decision | `PROMOTE` only to source-private adapter contract shaping. Do not promote a public backend selector or production integration from the directional numbers. |

## IOE-X6 Source-Private TCP Semantic Adapter — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | Linux-only, default-off, non-installed `IoUringTcpConnectionAdapter` projects one shared-Hub route onto the existing `TcpSendResult`, `TcpConnectionCloseInfo`, close phase, low/high/hard backpressure, high-water/write-complete callbacks, and terminal-future vocabulary. It adds no stable header, selector, listener, or production `TcpConnection` branch. |
| Commit | Exact implementation checkpoint `522d72cfc7fa4eea2434d7207d302a3608b59ef0`; parent IOE-X5 checkpoint `d400a9550b2a57e47fef319625dfadc163e6c21c` |
| Owner/lifetime | One EventLoop owner configures, establishes, sends, pauses/resumes, invokes callbacks, and force-closes. The Adapter borrows its Hub and owns only semantic observer state; Hub retains the transferred socket, operation identities, bytes, and physical future. Destroying the Adapter nulls the observer and requests route close, while a shared terminal state survives until the Hub future proves socket/operation retirement. Adapter future publication precedes surviving close callbacks. |
| Semantic contract | Local hard pressure maps to `Overloaded`, Hub aggregate pressure to `LoopOverloaded`, stale/closing to `Closed`, and empty open send to `Accepted`. High crossing pauses Recv and queues one bounded notification; low crossing resumes; zero queues write-complete. First semantic close info wins and preserves Hub native error. Message/high/write/close callbacks are owner-only, re-entrant, bounded where queued, and exception-contained. |
| Cross-backend contract | One test drives production epoll `TcpConnection` and the Adapter over separate real loopback TCP connections through 5 MiB hard-limit rejection, 4 MiB accepted output, high-water read pause, delayed peer input, low-water resume, callback-reentrant sends, write-complete re-entry, forced close, post-close rejection, and zero residue. A second case destroys the Adapter with Recv pending and observes no later adapter callback while its future and Hub still converge. Normal focused repeat passed 20/20; ASan/UBSan and TSan focused repeats passed 10/10 each. |
| Linux full gate | Exact commit under WSL Ubuntu 24.04/GCC 13.3 with experimental and ASan/UBSan leak detection/halt-on-error: 135/135 passed in 57.79 s. Inventory is 8 unit, 113 contract, 14 integration, 108 threading, 113 lifecycle, and 6 experimental tests. |
| Sanitizer/race evidence | Exact commit TSan threading inventory passed 108/108 in 53.50 s. Focused and full gates reported no leak, sanitizer, race, timeout, callback-after-observer, byte-accounting, fd-ownership, or residual-state finding. |
| Windows full gate | Exact commit, experimental-off: MSVC Debug 129/129 passed in 57.97 s and Release 129/129 passed in 59.06 s. This proves the Linux-only target does not enter the IOCP/default graph; it is not an io_uring runtime claim. |
| Benchmark | Exact commit Release shared-Hub sample at 256 connections, 100 RTT/connection, 64-byte payload validated 226,644.144 RTT/s, 27.667 MiB/s, P50/P99/P999 1,090/1,455/1,657 us, 3,616 active bytes/connection, 0.820 ms shutdown, and zero rejection/fallback/residue. It is a directional regression smoke, not a backend comparison or production threshold. |
| Public/governance | Exact tree: all 37 standalone repository guards plus migration provenance and public API comparison passed; `git diff --check` passed; 36 active intents and 184 verification paths passed. The v0.3 comparator reports `has_changes=false`, no compatibility decision, and no stable-surface review. Default inventory remains 129; experimental inventory is 135. |
| Remote evidence | Direct push verified `refs/heads/main` at implementation `522d72cfc7fa4eea2434d7207d302a3608b59ef0`; no remote CI, endurance, release, or production integration result is claimed. |
| Decision | `integrate` and close IOE-X6 as forced-close/backpressure adapter evidence. Advance immediately to IOE-X7 graceful pending-output drain and half-close equivalence; keep forced close distinct, epoll production-default, and all selectors/install changes deferred. |

## IOE-X7 Graceful Drain and TCP Half-Close — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | The Linux-only, default-off, non-installed Hub/Adapter adds an explicit `GracefulDraining` route phase and owner-only `tryShutdown`. A graceful request seals new send admission, preserves every already accepted byte, executes one native `shutdownWrite` after output reaches zero, and keeps Recv active until peer terminal or an explicit physical escalation. |
| Commit | Exact closure checkpoint `485b2508471bc17a4754524cdce5eefbda59c97d`; implementation commit `8ce4d8e489570e1ff66ad7cc75b5925a5356959c`; parent IOE-X6 evidence checkpoint `2066684b62b1a8b1c779b5707de560abc06bec2a` |
| Owner/lifetime | One EventLoop owner mutates Adapter, route, Send/Recv identities, graceful phase, native half-close, metrics, callbacks, and terminal state. `shutdownWrite` releases neither socket nor route future. Hub keeps the socket and outstanding operations until terminal CQEs retire, closes the socket once, fulfills the Adapter future, then invokes surviving close observers. |
| Semantic contract | Repeated graceful requests are idempotent. New sends are `Closed` after the first request; accepted output drains before one write half-close; inbound delivery remains enabled after local EOF. First close info wins, so graceful followed by force, peer reset, callback failure, or owner quit retains `GracefulShutdown` while the later event only accelerates physical retirement and may enrich native error. |
| Cross-backend contract | Production epoll `TcpConnection` and the Adapter each queue 2 MiB behind a constrained real TCP send buffer, request shutdown twice, reject late send, deliver the complete payload plus local EOF to the peer, then deliver the peer's post-EOF `reply` before peer EOF closes the connection. Adapter-only real-TCP cases cover graceful-to-force escalation, reset with pending work, throwing message callback after half-close, and EventLoop quit with pending Send/Recv. Every case asserts one semantic terminal, future-before-close-callback, exact accepted-byte accounting where drain is possible, and zero Hub/Engine residue. |
| Focused contracts | Exact closure tree passed the expanded Adapter contract 10/10 normally, 10/10 under ASan/UBSan, and 10/10 under TSan. No timeout, duplicate terminal, reason overwrite, post-half-close read loss, callback escape, race, leak, or residual operation/byte state was reported. |
| Linux full gate | Exact closure checkpoint under WSL Ubuntu 24.04/GCC 13.3 passed 135/135 normally in approximately 52.6 s and 135/135 under ASan/UBSan in 57.72 s. Inventory remains 8 unit, 113 contract, 14 integration, 108 threading, 113 lifecycle, and 6 experimental tests. |
| Sanitizer/race evidence | Exact closure checkpoint TSan threading inventory passed 108/108 in 54.43 s. The focused terminal matrix and the full threading gate reported no race, callback-after-retirement, stranded CQE/lease, fd ownership, or byte-accounting finding. |
| Windows full gate | Exact closure checkpoint, experimental-off, passed MSVC Debug 129/129 and Release 129/129; the Release run completed in 51.28 s. This proves X7 remains absent from the default IOCP graph and makes no Windows io_uring runtime claim. |
| Benchmark | Exact closure checkpoint Release shared-Hub regression smoke at 256 connections, 100 RTT/connection, and 64-byte payload validated 237,501.264 RTT/s, 28.992 MiB/s, P50/P99/P999 1,070/1,146/1,158 us, 3,648 active bytes/connection, 0.786 ms shutdown, and zero rejection/fallback/residue. It is directional, not a public-backend promotion threshold. |
| Public/governance | All 37 standalone repository guards plus scope boundary, migration provenance, manifest verification, and public API comparison passed; `git diff --check` passed; 36 active intents and 184 explicit verification paths passed. The v0.3 same-line comparator reports `has_changes=false`, no compatibility decision, and no stable-surface review. No installed header, target, selector, listener ownership, or production `TcpConnection` branch changed. |
| Remote evidence | Direct pushes verified `refs/heads/main` first at implementation `8ce4d8e489570e1ff66ad7cc75b5925a5356959c` and then at exact terminal-matrix closure `485b2508471bc17a4754524cdce5eefbda59c97d`. No remote CI, endurance, release, or production integration result is claimed. |
| Decision | `integrate` and close IOE-X7. Advance immediately to IOE-X8 source-private cross-thread admission/lifecycle equivalence; keep EventLoop/Hub ownership fixed and defer listener ownership, installed surfaces, public backend selection, and production replacement. |

## IOE-X8 Bounded Cross-Thread Admission — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | The Linux-only, default-off, non-installed Adapter adds one fixed-capacity command mailbox and one coalesced EventLoop lifecycle source. Only `trySend`, `tryShutdown`, and `tryForceClose` admit from foreign threads; Hub routes, socket syscalls, callbacks, and physical retirement remain on the original owner. |
| Commit | Exact implementation and contract checkpoint `978aacdcb04ab2ba0af86f6462d63ae3b55b6385`; parent IOE-X7 closure `06459b89d04b1bfb6b670bf7dc395e9ba5a0fef0` |
| Ownership/order | A successful foreign send transfers a copied payload, one command slot, and an overflow-safe byte reservation to shared Adapter state. One mutex linearizes owner/foreign Send/Shutdown/Force admission: every accepted send before the first terminal command drains first, that terminal seals later send, repeated terminal requests are idempotent, and the first admitted close reason remains stable. Rejection retains no payload. Accepted work either reaches the Hub or is counted as terminal-cancelled bytes. |
| Pressure/fairness | `maxPendingCommands` and `maxCommandsPerTurn` independently bound retained commands and owner work per lifecycle callback. Hard byte pressure returns `Overloaded`, full mailbox returns `SchedulingQueueFull`/`QueueFull`, terminal seal returns `Closed`, and EventLoop closure maps to owner shutdown/unavailability. A lifecycle continuation re-signals while commands remain; no foreign call waits for the EventLoop or invokes user code. |
| Real-TCP contracts | Production epoll and Adapter foreign callers run the same real loopback send, owner-turn handoff, graceful request, full peer payload, local EOF, peer reply, and terminal sequence. Adapter-only cases additionally prove sequential foreign send-before-shutdown ordering, deterministic two-slot saturation/recovery with exact payload order, concurrent graceful/force first-reason retention, queued-payload observer revocation, and owner quit reconciliation. Callbacks stay owner-affine and every case converges command, route, operation, notice, fd, and byte state to zero. |
| Focused contracts | Exact checkpoint passed the expanded Adapter contract 10/10 normally, 10/10 under ASan/UBSan, and 10/10 under TSan. The concurrent terminal race uses two actual foreign workers and a barrier; no race, leak, duplicate terminal, ordering failure, callback-after-destroy, timeout, or residue was reported. |
| Linux full gate | Exact checkpoint passed 135/135 normally and 135/135 under ASan/UBSan with the experimental graph enabled. Inventory remains 8 unit, 113 contract, 14 integration, 108 threading, 113 lifecycle, and 6 experimental tests. |
| Sanitizer/race evidence | Exact checkpoint TSan threading inventory passed 108/108 in 55.17 s. Full and focused sanitizer gates reported no data race, use-after-free, leak, reservation drift, command-slot leak, callback affinity violation, or stranded Hub/Engine obligation. |
| Windows full gate | Exact checkpoint, experimental-off, passed MSVC Debug 129/129 and Release 129/129; Release completed in 51.35 s. This proves the source-private Linux target and its headers do not enter the default IOCP graph and makes no Windows io_uring runtime claim. |
| Benchmark | No new X8 throughput benchmark is claimed. The mailbox is lifecycle/control-plane work, while Hub data-plane code and the production path were unchanged; focused fairness and full regression gates are the closing evidence. The earlier X7 Hub number is not relabeled as exact X8 performance evidence. |
| Public/governance | All 38 standalone Python guards plus migration provenance and scope-boundary checks passed; `git diff --check` passed; 36 active intents and 184 explicit verification paths passed. The v0.3 same-line comparator reports `has_changes=false`, no compatibility decision, and no stable-surface review. No installed header, target, selector, listener ownership, or production `TcpConnection` branch changed. |
| Decision | `integrate` and close IOE-X8. Advance immediately to IOE-X9 source-private listener/Accept ownership with bounded one-shot admission, exact accepted-fd retirement, real multi-client burst/churn, and listener-first stop; continue to defer public backend selection and production replacement. |

## IOE-X9 Source-Private Listener / Accept Ownership — 2026-08-20

| Field | Evidence |
| --- | --- |
| Slice | One Linux-only, default-off, non-installed listener lives inside the existing shared Hub/Pump. A finite `maxPendingAccepts` one-shot window generation-routes Accept beside Recv/Send without a second ring, Channel, worker, or unbounded side queue. |
| Commit | Exact implementation and contract checkpoint `24ca10e4ec9a6358050d8aa10cec060a42925275`; parent IOE-X8 closure `f916ad09ab30abba9905e0b3fc4eeaa90b924c9a` |
| Owner/lifetime | The Hub owner alone configures, listens, submits/cancels Accept, invokes the callback factory, transfers accepted fds, publishes metrics/futures, and destroys listener state. Every `listen` call transfers the input fd. A successful notice is immediately RAII-owned; the factory receives callbacks only, and only successful route admission transfers the fd again. All rejection/re-entry/failure paths close exactly once. |
| Admission/stop | Accept identities use the existing fixed operation-route table and Pump dispatch budget. Connection-limit rejection is local and rearms; invalid factory, callback failure, fatal Accept, or inability to arm the first operation fails the listener closed with typed outcome/reason. `stopListening` closes admission first, cancels exact Accept generations, and publishes only after zero active Accepts. Hub stop performs this transition before route close and Pump quiesce. |
| Real-TCP contract | `tests/contract/io_engine/test_io_uring_tcp_listener.cpp` compares production epoll `TcpServer` and the completion listener through bind/connect/echo/peer-close/stop. Hub cases cover a two-slot Accept burst, overflow fd rejection, route-capacity recovery, slot generation reuse, stale identity rejection, invalid/duplicate listener transfer, deterministic first-arm operation pressure while existing routes progress, stop re-entry from the factory, throwing/invalid factories, explicit listener stop, and EventLoop quit. |
| Focused evidence | The listener contract passed 100/100 normally, 20/20 under ASan/UBSan with leak detection/halt-on-error, and 20/20 under TSan. All seven experimental Engine/Pump/driver/Hub/capacity/listener/adapter contracts also passed focused normal, ASan/UBSan, and TSan runs. |
| Linux full gate | Exact implementation checkpoint passed 136/136 normally and 136/136 under ASan/UBSan. Inventory is 8 unit, 114 contract, 14 integration, 109 threading, 114 lifecycle, and 7 experimental tests. |
| Race gate | Exact checkpoint TSan threading inventory passed 109/109. No race, leak, double-close, stale generation callback, stranded Accept, route/operation residue, or shutdown-order finding occurred. |
| Windows gate | Exact checkpoint, experimental-off, passed MSVC Release 129/129. This proves X9 remains absent from the default IOCP graph; it is not a Windows io_uring claim. |
| Public/governance | IOE-X9 static governance, intent consistency/metadata/semantics, scope guard, build/workflow registration, migration provenance, and API manifest contracts passed. The derived inventory is 36 active intents and 185 explicit verification paths. The reviewed v0.3 compatibility comparison reports `has_changes=false`, no compatibility decision, and no stable-surface review. |
| Decision | `integrate` and close IOE-X9. Advance to IOE-X10 fixed-capacity listener/connect/churn measurement and a `PROMOTE`/`DEFER` decision limited to further source-private shaping. Continue to defer installed listener APIs, public backend selection, multishot, and production replacement. |

## M1 / IOE-X10 Listener Decision and ARCH-G1 Closure — 2026-08-22

| Field | Evidence |
| --- | --- |
| Implementation/evidence commit | Exact IOE-X10 and ARCH-G1 fix-forward checkpoint `f5d39b800b4dd943531670aa09840c931c3dee4d`, pushed to `origin/main`; parent governance checkpoint `a5ff7e6d823984a86e89146889f29f6615702ec3` |
| Scope | Fixed comparison of the production epoll `TcpServer` listener and default-off, Linux-only, non-installed io_uring listener/Hub. No installed header/target, public selector, production replacement, multishot, registered buffer/file, SQPOLL, or advanced feature was added. |
| Protocol | 256 concurrent active routes; `maxPendingAccepts=32`; Hub route limit 256; four churn waves replacing 64 routes each; 100 64-byte echo RTTs per active route/wave; one full warm-up and five interleaved Release formal samples per backend; affinity CPUs 0 and 1 on the same machine/compiler/kernel. |
| Raw evidence | `docs/development/benchmark_results/2026-08-22-ioe-x10-f5d39b8/evidence.json` plus five epoll and five io_uring raw JSON samples. The manifest binds every sample by SHA-256; the migration-status contract recomputes all ten hashes and checks the fixed protocol, decision, scope, and exact commit. |
| Correctness/capacity | Every formal sample completed 516 connects, 512 admissions, 128,000 RTTs, 512 closes, four capacity rejections with recovery, and zero final listener/route/operation/notice/fd/pending-byte/Engine-owned-byte residue. io_uring high-water marks were Accept 32, Recv 256, Send 256, operations 544, with zero SQ rejection. Completion-only epoll fields remain `null`; they are not synthesized. |
| Medians | epoll: 444,597.429 RTT/s, 54.272 MiB/s, P50/P99/P999 223/626/1538 us, RSS 6,647,808 bytes. io_uring: 221,923.627 RTT/s, 27.090 MiB/s, P50/P99/P999 498/874/1176 us, RSS 6,340,608 bytes. io_uring/epoll ratios: RTT and throughput 0.499156, P50 2.23318, P99 1.39617, P999 0.764629, RSS 0.953789. |
| Independent review | `docs/reviews/arch-g1-independent-review.md`; reviewer `/root/arch_g1_review`; final decision `APPROVE` after the first-pass mutable-Hub observation and destructor-drain blockers plus second-pass standalone Engine destruction and Pump registration rollback gaps were closed. Approval is explicitly limited to the reviewed source-private seam and later shaping. |
| Linux gates | Exact IOE-X10 implementation tree: experimental Release 136/136; focused 7/7; focused repeat 20 rounds (140 passes); ASan/UBSan focused 7/7 with leak detection and halt-on-error. Plain GCC TSan was blocked before test code by WSL's `unexpected memory mapping`; the same binaries under `setarch x86_64 -R` passed focused 7/7 with no race report. Default-off Release passed 129/129. Stable API comparison was zero-diff. |
| Windows gate/fix-forward | Default-off MSVC Release first passed 128/129 and exposed a legal cadence-stop convergence race that could count one timer retirement twice. Intent/rules and the Profile C contract now require one published cancellation outcome whether the owner callback retires before a post or meets an accepted post. Linux and Windows focused repeats each pass 100/100; complete Linux/Windows default-off Release gates pass 129/129 after the fix. |
| Decision | `PROMOTE`, narrowly: authorize IOE-X11–X15 only when M6 is reached, after v0.3 promotion and real gateway validation. Preserve the measured throughput/P50/P99 debt. M1 closes; M2 exact-promotion-commit evidence becomes the sole implementation front. |

## M2 Internal Candidate Convergence Checkpoint — 2026-08-22

| Field | Evidence |
| --- | --- |
| Candidate | Exact promotion candidate `a89e2b0f13242a4cd4b49093f562b93adcd509a4`; production Linux/epoll and Windows/IOCP unchanged; stable API zero-diff. |
| Passing lanes | Main CI `32515776695/1` (7/7); dedicated-100k capacity `32515817027/1`; candidate-10k capacity `32515907929/1`; paired benchmark `32515904248/1`; repeat-50 `32515900974/1` with threading 5,100/5,100 and Pipeline/Broadcast 600/600. All canonical artifacts and independent manifest hashes are recorded in `m2_internal_candidate_checkpoint_2026-08-22.md`. |
| Package preflight | Exact-candidate source and Linux/Windows installed-package drafts were generated locally; archive inventory/path checks passed and extracted-package consumers passed 2/2 on each platform. They remain ignored local drafts, not a release. |
| Endurance | Candidate-24h `32516260909/1` was cancelled by task-convergence instruction after 21,073.016 seconds and 17,978 balanced fault cycles. Maximum RSS was 14,512,128 bytes with 9,547,776 bytes growth. Wrapper status is `cancelled`; current-result/promotion verification did not run. Release-72h was not dispatched; no waiver was used. |
| Cleanup | Temporary Linux/Windows runner services and GitHub registrations removed; WSL keepalive stopped; runner directories preserved. |
| Decision | `STOPPED / NO-PROMOTION`. M2 is not closed, `v0.3.0-internal-candidate.1` is not produced, and M3 remains blocked. The historical cancelled run remains invalid; the current policy requires a fresh complete 1h/3h evidence chain. |

## M2 v0.3.0 Internal Candidate Closure — 2026-08-23

| Field | Evidence |
| --- | --- |
| Candidate | Exact promotion commit `0c3012449ae36fa32656da33c4d1161f5129cde7`, tree `28ad31b057f6fa76c744bb9c9cd372b5a4cbbbd8`; no Core runtime path or stable API changed. |
| Platform/CI | Main run `32575344030/1` passed all seven jobs covering Linux Debug/Release, ASan/UBSan, TSan, Windows Debug/Release/IOCP, install consumers, guards, fuzz, and aggregate. |
| Capacity/performance | Candidate-10k `32575517520/1`, dedicated-100k `32575519054/1`, and paired benchmark `32575521055/1` passed for Linux/epoll and Windows/IOCP. Raw producer evidence independently regenerated the retained aggregates. |
| Repeat/fault | Run `32575522678/1` passed threading 5,100/5,100 and Pipeline/Broadcast 600/600; the selection includes the fault-injection integration contract. |
| Endurance | Candidate-1h `32576118286/1` passed 3,050 balanced cycles and release-3h `32580658838/1` passed 9,158. Same-commit v2 pair and release promotion independently revalidated; no waiver. Release supervisor elapsed 10,800.146 seconds, RSS maximum 14,475,264 bytes, growth 9,547,776 bytes. |
| Package/SBOM | Reproducible 562-file source tar/zip plus 68-file Linux/Windows binary packages; installed-tree and clean-extracted consumers passed 8/8. SPDX 2.3, notices, GitHub artifact inventory, 723-file evidence index, evidence ZIP, and `SHA256SUMS` passed schema/hash/path verification. Exact hashes are recorded in `docs/development/releases/v0.3.0-internal-candidate.1.md`. |
| Ownership/lifetime | Evidence/release tooling owns files only. EventLoop/TCP owners, callback affinity/re-entry, cross-thread admission, and release rules are unchanged from the validated runtime tree. |
| Cleanup | Temporary Linux and Windows repository runners stopped and unregistered after upload; runner directories retained. |
| Decision | `integrate` the internal engineering candidate and close M2. It grants no external-use permission, creates no GitHub Release/tag, and activates M3 real gateway integration in a separate private repository. |
