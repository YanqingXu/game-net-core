# v0.2.0-phase4-preview Release Preparation

> **Status: validated candidate, unpublished draft.** As of 2026-07-12, the
> functional candidate and its CI/fuzz/soak/benchmark evidence exist, but no
> `v0.2.0-phase4-preview` tag or GitHub Release exists. This document remains a
> proposed release-note source and must not be cited as a published Release.

## Candidate identity

| Field | Value |
| --- | --- |
| Intended version | `0.2.0` |
| Intended tag | `v0.2.0-phase4-preview` |
| Validated functional candidate | `5ebad2c1a4a9487437340935e21f7468140c7e8d` |
| PR head at candidate validation | `5ebad2c1a4a9487437340935e21f7468140c7e8d` |
| GitHub merge-ref tested by main CI | `e461b597f2642e000717f536f3b430b804ba26ad` |
| Main-branch release commit | **TBD** |
| GitHub Release URL | **TBD — not published** |

PR run `29160903594` tested the merge-ref while binding candidate and PR head
`5ebad2c1...` in `gamenet.ci_evidence_set.v1`. Manual long-soak run
`29161167423` and benchmark run `29161168417` directly tested
`5ebad2c1...`. The historical draft-PR run on `0d62054` remains an earlier
74-test baseline and is not final evidence.

Candidate creation and authoritative candidate runs are complete. Review,
changing the PR out of Draft, merging, tagging, and publishing remain explicitly
unperformed and require separate authorization. Any evidence-documentation
descendant must be proven non-functional and pass the necessary PR/main gate;
ancestry alone does not transfer candidate evidence.

## Intended release position

This preview extends the frozen Reactor/TCP core with the first installed game
network foundation components. It is an integration and contract preview, not
a production-readiness declaration.

Installed CMake targets intended for this preview:

- `GameNet::core`
- `GameNet::protocol`
- `GameNet::transport`
- `GameNet::game_session`
- `GameNet::game_logic`
- `GameNet::broadcast`

`gamenet_game_server_pipeline_demo` remains a non-installed example.
`gamenet_phase4_benchmark` remains an opt-in, non-installed, non-CTest
engineering executable.

## Proposed highlights

### Packet framing

- Unsigned 32-bit big-endian length-prefixed framing.
- Explicit payload, buffered-byte, frame-count, and processed-frame-byte
  limits.
- `BudgetExhausted`/`needsContinuation` fairness contract and ring-buffered
  unread storage.
- Deterministic segmented round-trip coverage plus an optional differential
  libFuzzer harness.

### Transport lifetime boundary

- Narrow TCP endpoint adapter with stable transport identity.
- Copyable, non-owning `EventLoopExecutor` scheduling capability.
- Explicit `WrongThread`, `OwnerUnavailable`, and `Closed` results.
- Cross-thread `isOpen()` snapshot without a cached dereferenceable loop
  pointer.
- Final-drain semantics: work admitted before EventLoop shutdown may complete
  owner-only endpoint operations, while admission after closure is rejected.

### Session and logic foundation

- Management-loop-owned player and transport indexes.
- Explicit duplicate-login and transport-id/endpoint collision behavior.
- Manager public results expose read-only `PlayerSession` views, preventing
  external rebind/state mutation behind the indexes.
- Bounded cross-thread command admission, fixed-size tick drains, one-shot
  lifecycle, and stop-time backlog accounting.

### Three-loop Pipeline example

- Real TCP flow from I/O-owned framing through management-owned authentication
  and a separately owned LogicLoop back to the endpoint owner loop.
- Explicit `Unauthenticated -> Authenticating -> Online -> Closing` state.
- Bounded pending input while authentication is outstanding.
- Same-write `AUTH + command` ordering and revocable callback admission.
- Callback-state ownership does not preserve execution permission after
  revocation; every loop/timer/output/endpoint side effect rechecks the shared
  alive state.
- Configurable duplicate-login policy with deterministic rejection and queued
  pre-auth input discard coverage.
- Safe synchronous `stop()` re-entry from the Logic stage when management and
  logic share one EventLoop, plus endpoint-observer revocation before send.

### Broadcast

- Management-loop routing with shared payload ownership and endpoint
  deduplication.
- Hard/soft fanout and byte policies with reason-specific metrics.
- Move-only, router-created plans and executor-based per-owner dispatch.
- Bounded task fanout/bytes, multi-loop real TCP delivery, and versioned Phase 4
  latency/memory benchmark output.
- Eight real TCP disconnect/reconnect cycles prove stale endpoints are not
  retained in subsequent route plans.

### IOCP lifecycle corrections

- Acceptor completion storage remains alive until canceled completion dequeue.
- Connector ConnectEx timeout/stop cancellation retains operation and Channel
  lifetime until a late completion packet is consumed, including the
  `CancelIoEx` completion-won race.

## Local preflight evidence available

The following results are useful development evidence but are not substitutes
for final candidate-SHA remote evidence:

- Current configured inventory: 85 CTest tests.
- Windows MSVC Debug: final-v4 85/85 configured CTests passed in 36.47 seconds.
- Windows MSVC Release: final-v4 85/85 configured CTests passed in 36.97 seconds.
- Linux Clang 19 Release: final-v4 85/85 configured CTests passed in 34.76 seconds.
- Focused SessionManager contracts: MSVC Debug 2/2 and Release 2/2 passed.
- Full Windows MSVC Debug ASan: final-v4 85/85 passed in 43.19 seconds after the Connector
  late-completion UAF found by the first full run was fixed.
- Linux Clang 19 ASan/UBSan: final-v4 85/85 passed in 36.18 seconds; the subsequently
  added live-session heartbeat/offline post race also passed sanitizer coverage.
- Linux Clang 19 TSan: final-v4 all 61 threading-labelled tests passed in 35.63 seconds
  after TSan exposed and the worktree fixed a TimerQueue contract-test handle
  initialization race.
- PacketFramer libFuzzer: 1000 local Clang 19 ASan/UBSan runs passed with six
  binary seeds, the five-entry dictionary, fixed seed `424242`, `max_len=8192`,
  and `timeout=2`.
- Linux Clang and Windows MSVC Release package installation and external
  `find_package(GameNetCore 0.2.0 EXACT)` consumers: 1/1 CTest passed on each
  platform while linking all six exported targets and exercising transport and
  session APIs. The current-worktree Windows Release external consumer passed
  1/1 in 0.02 seconds.
- Linux Clang/epoll and Windows MSVC/IOCP each produced the three final-v4 fixed
  Phase 4 Release `gamenet.phase4_benchmark.v1` scenarios with `status: ok`;
  both raw sets pass semantic metric and count-invariant validation. Windows
  framing recorded 10,386,847.758 operations/s and 2,575.474 MiB/s, Logic
  accepted 20,000/rejected 0 with P99 596,206.300 us, and Broadcast accepted
  32,768/dropped 0 with completion P99 435.000 us. Linux framing recorded
  38,087,693.105 operations/s and 9,444.046 MiB/s, Logic recorded 452,934.439
  operations/s with P99 37,832.501 us, and Broadcast recorded 5,036,891.294
  operations/s with completion P99 262.500 us. These pre-freeze local results
  came from different machines and must not be compared as platform scores;
  the authoritative remote pair is recorded separately below.
- Clean-checkout workflow contracts pin `YanqingXu/mini_trantor` to
  `3eba368475a68f677aae920d4f299b155db23d57` and validate all 20 declared
  Phase 4 source paths against that Git object before intent semantics.
- Local Windows benchmark JSON is recorded in
  [the 2026-07-11 snapshot](../benchmark_results/2026-07-11-windows-msvc-release-phase4/README.md).
- Local Linux benchmark JSON is recorded in
  [the 2026-07-11 Clang snapshot](../benchmark_results/2026-07-11-linux-clang-release-phase4/README.md).

After the final Pipeline callback/lifetime repairs, the current worktree passed
the final-v4 61-test threading selection × 50 (3,050 executions, zero failures,
1,777.76s) and the 8-test Pipeline/Broadcast selection × 50 (400 executions,
zero failures, 54.16s). Both `gamenet.ctest_repeat_evidence.v1` summaries
validated successfully. These results came from the pre-freeze worktree and
remain local diagnostic preflight; the candidate-SHA remote run below is the
authoritative repeat evidence.

## Candidate remote evidence

- Main CI [`29160903594`](https://github.com/YanqingXu/game-net-core/actions/runs/29160903594), attempt 1: six producers plus aggregate, 7/7 success. The aggregate artifact
  `ci-evidence-set-e461b597f2642e000717f536f3b430b804ba26ad-29160903594-1`
  has ZIP SHA-256
  `408398ea5fdcf05675c819136a1ab5665d4dbe1fbcc5a537f45e5832d10cbf3a`.
  Its manifest records five 85/85 full suites, TSan 61/61, three executed
  install consumers, and 1,000 real libFuzzer units.
- Long-soak [`29161167423`](https://github.com/YanqingXu/game-net-core/actions/runs/29161167423), attempt 1: threading 3,050/3,050 and Pipeline/Broadcast 400/400.
  Artifact ZIP SHA-256 is
  `1e54b26681e39ff72dfb3dec3b9fddd2f0caa180871b4aa7f21051672c57b8c4`;
  both raw logs and structured repeat manifests were independently reverified.
- Benchmark [`29161168417`](https://github.com/YanqingXu/game-net-core/actions/runs/29161168417), attempt 1: Linux/epoll producer, Windows/IOCP producer, and pair gate
  all succeeded. Pair artifact ZIP SHA-256 is
  `a4ce4eda074928c1ca97fdb6171752fcf33028f684bd75e3b733a5e7734ce9a8`;
  the downloaded pair manifest matches local recomputation except for its
  generation timestamp.

The detailed artifact names, producer hashes, exact commands, inventories, and
identity boundary are recorded in
[`phase4_evidence_ledger.md`](../phase4_evidence_ledger.md).

## Evidence contracts implemented and exercised

These contracts define and have now produced the candidate evidence above.
They still do not authorize merge, tag, or publication by themselves.

- Main `ci` has six producers (`linux-cmake`, `linux-asan-ubsan`, `linux-tsan`,
  `linux-release`, `windows-msvc`, and `windows-msvc-release`) plus one
  aggregation-only `ci-evidence-set` job. Each producer writes
  `gamenet.ci_evidence.v1` with a canonical
  `<prefix>-<job>-<sha>-<run_id>-<run_attempt>` artifact identity. The
  `gamenet.ci_evidence_set.v1` aggregate accepts exactly those six jobs from one
  run/attempt/SHA, rehashes every declared file, and verifies exact inventory
  and JUnit selections. It is not a seventh platform producer.
- `long-soak` derives the 61 threading and 8 Pipeline/Broadcast selections from
  the 85-test inventory. `gamenet.ctest_repeat_evidence.v1` requires every
  selected test to pass exactly 50 times, rejects missing/extra/non-passing
  results, records total and per-test executions, and hashes the inventory and
  raw log. The canonical long-soak artifact carries both raw logs and both
  structured summaries.
- `core-benchmark` has Linux epoll and Windows IOCP producers plus an
  aggregation-only pair gate. Each platform preserves the three raw scenario
  JSON files, semantic manifest, toolchain, and `gamenet.ci_evidence.v1`
  wrapper. `gamenet.phase4_benchmark_pair_evidence.v1` requires the exact pair,
  common run/SHA identity, correct runner/platform/backend, identical scenario
  parameters and order, and matching hashes.

## Publication gates

Candidate evidence gates are checked below. Publication remains unchecked until
review, merge/main identity, tag, checksums, and the Release itself are complete.

- [x] Freeze and record candidate, PR-head, and merge-ref SHAs.
- [x] Pass PR merge-ref Linux Debug and full CTest while binding candidate/PR head.
- [x] Pass PR merge-ref Linux ASan/UBSan and full CTest while binding candidate/PR head.
- [x] Pass PR merge-ref Linux TSan threading selection.
- [x] Pass PR merge-ref Linux Release and full CTest.
- [x] Pass PR merge-ref Windows MSVC IOCP Debug, install, and executed consumer.
- [x] Pass PR merge-ref Windows MSVC IOCP Release, install, and executed consumer.
- [x] Execute the real PacketFramer libFuzzer target under ASan/UBSan and retain
  its exact command, corpus, duration/run count, and result.
- [x] Refresh the local final-v4 61-test and 8-test repeat-50 selections after
  the final Pipeline lifetime fixes: 3,050 executions in 1,777.76s and 400
  executions in 54.16s, with both structured summaries successful. This remains
  pre-freeze local diagnostic evidence.
- [x] Run the complete remote threading inventory and Pipeline/Broadcast slice
  with repeat-until-fail 50 and preserve raw logs, structured summaries, and the
  canonical job manifest.
- [x] Produce paired Linux epoll and Windows IOCP Release artifacts for framing,
  logic queue, and broadcast fanout from the same candidate SHA.
- [x] Preserve the canonical six-producer `gamenet.ci_evidence_set.v1`
  aggregate from one successful current-candidate run.
- [x] Record all run IDs, job names, test counts/labels, artifact names, dates,
  durations, and conclusions in the evidence ledger.
- [ ] Merge without rewriting the validated candidate and rerun required
  main-branch gates.
- [ ] Create and verify the annotated `v0.2.0-phase4-preview` tag.
- [ ] Generate and record source/archive checksums for the final tag.
- [ ] Publish the formal GitHub Release with final notes and artifact links.

## Known limitations

- Public Phase 4 APIs are preview-grade and may change before a stable release;
  no source or ABI compatibility promise is made yet.
- The Pipeline is an example, not an installed production shutdown coordinator.
  It revokes callback/output admission and coordinates LogicLoop teardown, but
  it does not expose a public asynchronous completion/future proving every
  server close callback has completed when `stop()` returns.
- LogicLoop is intentionally one-shot. Its owner must preserve the object
  through timer and handler/output/metric callback completion; restart after
  stop is rejected.
- Transport `send()` and `close()` remain endpoint-owner-loop operations.
  Cross-thread callers must use `ownerExecutor()` and handle rejected admission.
- PacketFramer is caller-owned and intentionally not thread-safe. Its configured
  bounds must be selected for the deployment and protocol.
- Broadcast does not compute AOI, room, team, or world membership. Real-TCP
  reconnect, slow-peer isolation, bounded high-fanout contracts, candidate
  long-soak, and paired benchmark evidence pass; they are Preview-grade
  validation rather than a production capacity guarantee.
- Benchmark numbers are raw comparison snapshots, not correctness thresholds,
  capacity guarantees, or production sizing claims. Framing currently reports
  throughput rather than allocator-level allocation counts, and process RSS is
  sampled only as a coarse process-level observation.
- TLS, HTTP, WebSocket, RPC, coroutine bridges, UDP, and KCP remain deferred or
  experimental and are not included in this preview.
- Candidate-level TSan, sanitizer-backed libFuzzer, repeat-50 long-soak,
  six-producer CI aggregate, and paired benchmark evidence exist. Main-branch
  verification, the annotated tag, source/archive checksums, and the GitHub
  Release do not yet exist.

## Preview API and downstream upgrade notes

Downstream consumers preparing for this preview should account for these
changes:

1. Use `find_package(GameNetCore REQUIRED)` and link only the installed targets
   needed by the application.
2. Replace cached/raw endpoint owner-loop use with `ownerExecutor()` and handle
   executor admission failure plus `EndpointResult::OwnerUnavailable`.
3. Treat `SessionManager::authenticate()` and `findBy*()` session pointers as
   `shared_ptr<const PlayerSession>`; all lifecycle mutation remains manager
   owned.
4. Handle PacketFramer budget and fault statuses explicitly. Continue buffered
   work with `push({})` only when `needsContinuation` is true.
5. Treat LogicLoop as one-shot and consume the returned stop/drop summary.
6. Obtain `BroadcastPlan` only from `BroadcastRouter::route()` and move it into
   `BroadcastDispatcher::dispatch()`.
7. Do not depend on Pipeline headers or targets as an installed framework API.

The following preview surfaces should be called out as unstable in the formal
release: `EventLoopExecutor`, PacketFramer budget/result types,
`TransportEndpoint`, SessionManager result/view types, LogicLoop lifecycle and
metrics, Broadcast plan/reason/metric types, and the example-only Pipeline
options/stage observer.

## Release artifact manifest

| Evidence/artifact | Required final value |
| --- | --- |
| Source tag and peeled commit | **TBD — tag not created**; validated candidate is `5ebad2c1a4a9487437340935e21f7468140c7e8d` |
| Required CI run and six-producer aggregate manifest | run `29160903594` / attempt 1; `ci-evidence-set-e461b597f2642e000717f536f3b430b804ba26ad-29160903594-1`; ZIP SHA-256 `408398ea5fdcf05675c819136a1ab5665d4dbe1fbcc5a537f45e5832d10cbf3a` |
| Long-soak run, raw logs, and two structured repeat summaries | run `29161167423` / attempt 1; 3,050/3,050 and 400/400; artifact ZIP SHA-256 `1e54b26681e39ff72dfb3dec3b9fddd2f0caa180871b4aa7f21051672c57b8c4` |
| PacketFramer libFuzzer command/result | CI `linux-asan-ubsan`; 1,000 units; producer artifact ZIP SHA-256 `dc4a8177b33df5de707ccfa4927d372925058bc8d4c402f9b44c9e552b9048e6` |
| Linux Phase 4 benchmark producer artifact | run `29161168417`; `phase4-benchmark-linux-release-benchmark-5ebad2c1a4a9487437340935e21f7468140c7e8d-29161168417-1`; ZIP SHA-256 `0a7ebecff8889a19688da57e3f44f6726e4e9e9731f69e5d8a545bcc75344978` |
| Windows Phase 4 benchmark producer artifact | run `29161168417`; `phase4-benchmark-windows-release-benchmark-5ebad2c1a4a9487437340935e21f7468140c7e8d-29161168417-1`; ZIP SHA-256 `cc77f51a94649afdd3e616f816cf7014effed7a8d7dceb28185dd90e43809e8b` |
| Paired benchmark aggregate manifest | `phase4-benchmark-pair-5ebad2c1a4a9487437340935e21f7468140c7e8d-29161168417-1`; ZIP SHA-256 `a4ce4eda074928c1ca97fdb6171752fcf33028f684bd75e3b733a5e7734ce9a8` |
| Package-consumer Linux/Windows results | main CI: Linux Debug 1/1, Windows Debug 1/1, Windows Release 1/1 |
| Source/archive checksums | TBD |

Only after every required value is immutable and linked should this preparation
document be converted into the final GitHub Release body.
